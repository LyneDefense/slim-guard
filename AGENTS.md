# SlimGuard 项目约定

## 云服务器与公网入口

- 使用 `ssh me` 登录 SlimGuard 云服务器。
- 项目公网基址为 `https://enceladus.online/`。
- 给出本项目的服务器命令或验收 URL 时，使用上述 SSH 别名和域名，不要再使用占位符。
- 知道登录方式不等于获得自动部署或切流授权；只在用户明确要求时执行服务器变更。

## 测试策略

- 日常开发不默认运行全量测试；根据改动模块、调用方和受影响的数据契约选择测试文件或具体用例。
- 小改动运行相关模块测试和必要的相邻回归。通常可控制在几十项至约 100 项，但不强制凑数量，也不随机抽取无关用例。
- 核心编排、Harness、鉴权、数据库迁移等跨模块改动应扩大回归范围；影响难以界定时可以运行全量测试，并说明原因。
- 重要版本发布前或用户明确要求时运行全量测试。仅修改文档时，通常只需检查差异和格式，无需运行业务测试。
- Ruff、Mypy、TypeScript 类型检查与构建属于静态/构建校验，不等于全量测试；可按需要执行。App 双端打包校验按移动端改动风险和发布需求安排，不默认每次都打包。
- 验收说明必须明确实际运行的范围、用例数量和结果；不能把定向测试说成全量通过，也不能把 `--collect-only` 收集结果说成测试通过。
- 不假定 Git 提交钩子会运行测试；说明提交等待原因前，应检查实际钩子配置和运行输出。
