# 表达风格：独立评测与反馈迭代设计

状态：历史讨论草案，仅供参考，非本轮需求或验收依据。日期：2026-09-18。

按用户最新要求，后续方案以刚才讨论的结论及 [风格训练器、Style Agent 与共享审查组件设计](STYLE_TRAINER_SHARED_REVIEW_DESIGN.md) 为主。本文保留旧讨论背景，不自动继承其中的固定题目配比、评分替换或动态选例等建议，也不代表这些建议已获批准或已经实现。

本文只提出设计，不授权代码修改、数据库清理、版本构建或服务器部署。

## 1. 先说结论

风格版本仍然是 **Style Guide（表达指引）＋表达示例快照**。调整的是版本的验收和改进方式：

- 素材库有 300 条，不代表需要人工评审 300 条；全部可用素材参与构建，人工每轮只看约 10～15 条测试。
- 评审题独立准备，不再把每条构建素材原封不动地变成评审题。
- 评审看这个版本在新输入上的实际表达效果，不是重新审批旧素材。
- 评分不会直接修改当前版本；通过“根据反馈重新构建”，产生一个新的候选版本。
- 页面说明指引改了什么、示例调整了什么、原来的问题是否修好。
- 评价可以更全面，但不把所有指标都变成管理员必须填写的评分项。

这是对 [表达风格系统重构设计](STYLE_PROFILE_REFACTOR_DESIGN.md) 中构建、评审、反馈部分的后续调整草案。页面层级继续遵守 [UI 设计](STYLE_UI_DESIGN.md)。[操作手册](STYLE_MANAGEMENT.md) 描述的是目前已经实现的系统，不应把本文的目标能力当作现有功能。

### 1.1 已明确的要求

1. 风格只转换表达，不做意图分类，不按“确认、询问、鼓励”等类别选话术。
2. 使用指引＋少量相关表达示例；没有合适示例时只使用指引。
3. 示例如实保留原回答和期望回答，素材库与线上版本分开。
4. 构建后提供约 10～15 条独立测试，而非逐条验收整个素材库。
5. 用户输入写成自然消息，不把长篇场景说明、模型分析拼进输入框。
6. 风格列表下仍只有“构建版本、评审版本、追加纠正素材”三个子 Tab；全部素材在追加素材页分页展示。
7. 接受时说明可选；拒绝时必须填写理由。
8. Style Agent 内部负责语义检查、有原因的有限重试和中性稿兜底。

### 1.2 本文提出、仍待确认的细节

- 默认每轮 12 条，采用第 4 节的固定题、新题、重点回归题配比。
- 人工评分改为“风格匹配、自然程度、交流分寸”；语义忠实单独检查，不混入平均分。
- 接受测试结果不再默认新增一条正向素材，避免测试集自动变成构建素材。
- 增加“根据反馈重新构建”和“这版改了什么”的操作与展示。

这些属于推荐方案，不表示用户已经逐项批准。

## 2. 当前系统的问题与保留能力

根据现有 `style_management/worker.py`、`runtime.py`、`repository.py`：

- 构建器逐条分析素材，再对每条可用素材生成一个 `ReviewCase`。人工工作量因此跟素材量一起增长。
- 当前评测虽然排除了本条及同原文示例直接进入提示，但 Style Guide 已经从这些素材中提炼，仍然属于素材回归，不是严格的独立测试。
- 当前评审不修改本版本的指引和示例；反馈在下一次构建中使用。
- 接受且自动检查通过的输出会自动加入正向素材；补充的期望回答也会新增素材。
- 构建前后指引变化、反馈采纳情况和同题输出变化缺少清晰展示。

需要保留的能力：多风格隔离、冻结版本、后台任务及失败恢复、发布与启用分离、Style Agent 内部校验、完整示例库分页、管理员认证和写操作保护。

## 3. 四种对象，不再混为一谈

| 对象 | 内容 | 用途 |
| --- | --- | --- |
| 构建素材 | 用户输入、原回答、期望回答、来源和质量信息 | 提炼指引、整理表达示例 |
| 风格版本 | 固定的指引、示例、来源快照和构建配置 | 运行时表达转换 |
| 测试用例 | 自然用户输入、必要上下文、中性回答、不可改变的事实 | 给不同版本出同一道题 |
| 评测结果 | 某版本在某题上的实际输出、自动检查、人工评分和反馈 | 决定是否发布、指导下一版改进 |

同一道测试题可以测试多个版本；同一个版本也可以接受补充评测。每次结果必须明确对应哪份版本产物和哪份冻结题目，不能刷新页面就换答案。

```text
构建素材 ──→ 指引＋表达示例 ──→ 候选风格版本
                                      │
独立测试集 ──→ 中性回答 ────────────────┤
                                      ↓
                           Style Agent 实际改写
                                      ↓
                           自动检查＋10～15 条人评
                            ↙                 ↘
                       满意：发布启用       不满意：反馈
                                               ↓
                                     根据反馈构建下一版
```

评测结果是版本的验收证据，不是运行时必须加载的知识，也不会直接把评审题的答案发送给真实用户。

## 4. 独立测试集怎么准备

### 4.1 默认每轮 12 条

建议分配如下，属于评测抽样策略，不是用户意图分类：

| 题目组成 | 默认数量 | 作用 |
| --- | --- | --- |
| 固定对照题 | 6 | 同题比较不同版本，发现退步 |
| 新生成题 | 3 | 检查未见过的输入和内容 |
| 重点回归题 | 3 | 检查上一版拒绝项和已知问题 |

首次没有回归题时，以新题补足。总数可选择 10～15，默认 12；不随素材库大小增长。

固定题不一定永远独立：如果其答案、纠正或反馈被后续构建采用，该题必须标记为回归题，不再计入独立测试。若仍保留在固定对照位置，应从其他位置保证有新的独立题，避免重复计算同一题。

如果历史问题超过当前人工名额，页面列出本轮未覆盖项；后台可以运行更多历史回归检查，但不偷偷扩充人工待评审列表。未解决的关键语义错误不能因为没抽中就被视为已解决。

### 4.2 题目来源与覆盖

独立题生成器使用产品能力边界、经过核对的测试事实和抽象覆盖要求，不读取本次素材原文、期望回答或候选 Style Guide。生成后再执行去重和相似性检查。

覆盖的是容易暴露表达问题的条件，例如：

- 极短回答与需要多句解释的回答；
- 名称、数字、单位、日期、否定条件和不确定性；
- 记录成功与记录失败；
- 已经表达清楚、不需要继续改写的原文；
- 容易被错误套入“不错，要保持”的普通问题；
- 找不到相关表达示例、仅使用 Guide 的情况；
- 不适合责备或反问的交流语境。

这些标签只服务于离线覆盖检查，不进入线上话术路由。初期只测风格环节，不宣称覆盖整个多轮教练、视觉识别或 RAG 的质量。

### 4.3 输入、上下文与中性回答分开

用户输入示例：

> 这顿帮我记一下。（带餐食图片）

必要上下文可以是：图片对应今天，但餐次尚未确定。它保存在独立字段，页面需要时展开，不拼到用户消息里。

中性回答示例：

> 需要确认这张图是午餐还是晚餐。

随后由待评审版本生成医生回答。测试生成器不能预先把“期望医生回答”写好，再让 Style Agent 照抄。

必须先检查题目自身是否合理：

- 用户已经明确说“这是午餐”，中性回答不能再追问午餐还是晚餐。
- 中性回答不能谎报工具成功，不能新增未提供的用户事实或专业结论。
- 图片描述只是合成测试条件；没有图片附件就不能宣称运行了视觉识别。
- 中性回答必须能回答该输入，并符合该题的上下文；否则这是坏题，不是风格不合格。

页面使用简短的“合成测试”来源标识即可，不恢复长篇“历史合成场景……”前缀。

### 4.4 中性回答的生成边界

题目准备与风格生成分开。先根据冻结的测试上下文生成中性回答，再验证其内容，最后交给 Style Agent。

初期使用隔离的中性回答生成器和已核对的测试事实，不执行真实记录、档案更新、提醒、长期记忆写入，也不为评测向真实用户发送消息。营养内容必须来自已核对的固定测试依据，不能让造题模型随意发明专业结论。

每题内部保存 `ResponsePlan` 或等价的结构化内容约束；数字、记录状态、风险及引用的保护方式与线上一致，不能把所有测试原文都当成无保护的闲聊文本。

### 4.5 独立性的判定

- 对用户输入、中性回答及已有答案做规范化去重，并检查近似复述；仅替换数字或菜名不能自动算一道独立新题。
- 向量相似度只是筛查信号，不把某个相似度阈值当成独立性的证明。
- 近重复题与原题属于同一题族；原题反馈用于构建后，近重复题也不能标记为未见题。
- 测试期望答案、人评意见不能进入当前版本的改写提示。
- 独立题可以检索该版本合法的表达示例，但不能检索测试答案或其衍生素材。
- 报告分别显示独立题和回归题结果，不合并成一个“泛化准确率”。

## 5. 如何运行评测

### 5.1 走真实的 Style Agent

冻结候选版本后，评测使用与线上相同的：

- Style Guide 和表达示例快照；
- 样例检索规则与回退策略；
- Style Agent Prompt、模型和结构化输入契约；
- 内部确定性检查、独立语义检查、带原因的有限重试；
- 中性稿兜底逻辑。

不另写一套只为评审生成好看回答的 Prompt。每题记录实际命中的示例、生成次数、检查原因、耗时和 Token；技术信息默认折叠。

### 5.2 对照比较

选择上一候选版或当前启用版作为对照。相同题目、中性回答和保护条件不变，只切换风格版本。

当前页默认只展示新版本医生回答；“查看上版回答”放在折叠区域，不恢复独立的旧 A/B 管理页面。

如果模型、系统 Prompt、检索策略也变了，应明确提示“比较不只涉及风格素材”。无法保持相同执行条件时，不得把所有变化都归功于新版 Guide。

### 5.3 兜底不能伪装成风格成功

- 直接生成并通过检查：正常结果。
- 返工后通过：可评审，同时统计返工。
- 校验失败后返回中性稿：显示“使用中性兜底”，不能作为风格迁移成功。
- 超时、接口错误、输出格式无效：执行异常，不是低风格分，也不能当作已完成的有效评审题。
- 原文本身合适，模型选择保持原文且通过检查：允许正常通过，不要求为了风格强行改字。

工作流继续使用有界超时、预算、租约和幂等恢复；页面展示完成条数、失败原因和实际消耗，不无限重试。

## 6. 评分设计：人工少填，系统多解释

### 6.1 推荐的三个主观评分

人工每条仍只填三项，采用 1～5 分：

| 维度 | 1 分 | 3 分 | 5 分 |
| --- | --- | --- | --- |
| 风格匹配 | 明显不像认可的医生表达 | 有部分特征，但仍泛化、机械或刻意模仿 | 用词、句式、节奏符合认可的风格，不靠套口头禅 |
| 自然程度 | 生硬、别扭或难理解 | 能读懂，但有赘述或不自然之处 | 顺口、清楚，长度符合表达需要 |
| 交流分寸 | 明显冒犯、敷衍或不合语境 | 大体合适，但轻重有偏差 | 直接程度、礼貌和情绪分寸合适 |

2、4 分表示相邻水平之间。界面可以查看评分说明，但不要求用户填写分析报告。评分不要求与某一句参考答案逐字一致。

“自然程度”与“交流分寸”不同：一句话可以很顺口，却带有不合适的责备；也可以足够礼貌，却像模板。

### 6.2 语义忠实单列为检查项

不建议把语义错误与风格分平均。语义忠实显示为“通过／存在问题／待复核”，同时支持人工指出漏检或误报。

检查内容包括事实、关键对象、数字、单位、时间、记录状态、否定条件、风险、不确定性、引用，以及新增建议。

判断含义是否保留，而非机械要求每个字都出现。例如“需要确认补发日期”改成“补的是哪一天的？”不能仅因词语减少就自动判为丢失语义；但数字、业务状态及明确风险等受保护信息不能随意省略。

人工标记“自动检查可能误判”后进入复核或重评，保存理由；不能直接改写原始自动结果，不能把未解决的语义问题靠高风格分放行，也不能绕过线上 Style Agent 内部检查。

### 6.3 版本级评价

这些结果在概览中显示，不要求每条再多打几项分：

| 项目 | 展示方式 | 注意事项 |
| --- | --- | --- |
| 语义问题 | 失败条数／有效检查条数，具体差异 | 不把调用失败计为语义通过 |
| 风格一致性 | 有偏差的题目及解释，供人工确认 | 风格一致不等于每句都一样短 |
| 新题表现 | 独立题的人评接受数、总数及评分分布 | 与回归题分开报告 |
| 套话／过度模仿 | 重复短语、出现题目、是否不合语境 | 重复词只是预警，不是自动定罪 |
| 返工／兜底／异常 | 各自条数和原因 | 不能只显示最后一次成功 |
| 版本变化 | 相同题目是否修好、是否有退步 | 不同题目不能直接算胜率 |

展示分布、分母和具体问题，初期不制造一个精确到小数点的“医生克隆率”。12 条只是开发阶段的验收窗口，不是大规模可靠性结论。

### 6.4 评价依据与模型局限

风格判断不能只检查“是否符合本次生成的 Guide”。需要结合用户认可的表达参考和固定评分说明；Guide 只是一种控制手段，不是证明效果的标准答案。

语义检查与风格评价使用分开的任务和输出。独立调用不等于独立模型；如果仍使用同一模型，应如实记录，不能称为无偏第三方裁判。

句长、反问频率、表情频率、普通语义向量等可做诊断，但都不能单独代表个人风格相似度。尤其普通语义相似度更接近“内容相似”，不等于“说话像同一个人”。

## 7. 人工操作与反馈如何真正改进版本

### 7.1 评审页的操作

- **接受**：保存三个评分和结论，说明可选。默认只表示这次测试通过，不自动扩充训练素材。
- **拒绝**：弹框填写必填理由，可补充期望医生回答；期望回答不是必填。
- **题目有问题**：指出输入、中性稿或上下文错误。这是题目修订，不给风格打低分，也不等同于跳过一个失败用例。
- **自动检查可能误判**：提交具体理由，进入复核；不静默改成通过。

独立测试通常没有预设的“期望医生回答”。该区域应为用户可选填写的纠正框，不由模型先生成一段答案当成人工标准。

有需要时，可以将接受的回答显式“加入示例库”；该操作不能与“接受”混为一谈。

### 7.2 根据反馈重新构建

增加显式按钮“根据反馈重新构建”。按钮旁说明：会创建新候选版本，不修改当前版本或线上版本。

新构建需要：

1. 指明要修订的候选版本，冻结它的 Guide、示例、反馈和本次完整可用素材库。
2. 区分反馈属于表达、语义漂移、题目错误还是上游内容问题。
3. 将明确的期望表达整理为纠正素材；只给分但没有文字依据，不能凭空推导规则。
4. 保留原始中性稿作为语义基准。若旧生成结果已经丢失信息，不能只比较“错误输出→期望输出”就把补回事实误判成新增建议。
5. 提炼指引变更、示例增删、冲突和暂不采纳项；说明证据。
6. 产生新版本，并再次运行固定对照、问题回归和新题。

用户期望里若新增专业事实或业务建议，应显示“内容纠正，不能直接用于纯风格学习”；不能因为人工填写就无条件变成风格规则。

一次拒绝可以修复某个具体问题，但不能直接推导成适用于所有输入的普遍规则。原文已经合适的正例可以帮助约束“不要过度改写”，但不能凭重复正例制造很高的规则置信度。

### 7.3 展示实际变化，而非仅显示分数变高

新版本页面应展示：

- 指引新增、修改、删除了哪些规则；
- 每项变化对应哪些素材或反馈；
- 示例新增、排除和去重情况；
- 哪些反馈被采纳、哪些未采纳及原因；
- 相同回归题的上版回答、本版回答和检查差异。

变更清单应由两份实际产物计算，再生成解释；不能仅让模型声称“已采纳反馈”。如果产物没变，应直接显示“本次未产生有效规则／示例变化”，不宣称有改进。

### 7.4 防止评测泄漏和素材自我膨胀

- 反馈保存与反馈被构建采用分开记录。
- 测试回答被加入示例库，或反馈被用于改写 Guide 后，该题及近重复题在后续版本中标为回归题。
- 评分汇总也可能影响构建，应记录反馈消费范围，不能只检查答案有没有复制进示例库。
- 相同反馈重复提交、任务重试、重复点击，不重复新增素材。
- 原始医生素材、用户明确修订和接受的模型输出应有不同来源标记；模型输出数量增加不能被当成新增的真人风格证据。
- 没有采纳进构建的独立题可以继续作为对照，但不无限靠同一组题调参并声称泛化。

## 8. 页面设计：仍然只有三个子 Tab

沿用 [既有布局与效果图](STYLE_UI_DESIGN.md)，本轮不新增后台主导航、独立 A/B 页或第四个“评测管理”Tab。

### 8.1 构建版本

- 分开展示“本次构建素材：300 条”和“人工验收：12 条”。
- 可查看 Guide、表达示例、构建进度、实际耗时和费用相关用量。
- 展示“这版改了什么”和问题回归摘要。
- 操作包括构建新版本、根据反馈重新构建、前往评审、采纳并发布、启用。
- 原则上默认即可完成任务；10～15 条数量选择放在可选设置中，不要求填写复杂配置。

### 8.2 评审版本

主要内容保持简洁：

```text
医生风格 · 当前候选版本
本轮验收 12 条：已接受 / 已拒绝 / 待处理 / 需修订题目

用户输入：这顿帮我记一下。（带餐食图片）
医生回答：……本版本实际生成的文字……

风格匹配 [1～5]    自然程度 [1～5]    交流分寸 [1～5]
语义检查：通过 / 有问题 / 待复核

期望医生回答 [选填]
[接受] [拒绝]

展开：必要上下文 / 改写前原文 / 上版回答 / 自动检查依据
更多操作：题目有问题 / 自动检查可能误判 / 加入示例库
```

套话预警、统一性和独立题／回归题统计放在本页概览。技术 Hash、检索命中、Prompt 等仍在折叠详情里。

例题冻结后不因刷新而变化。若确认题目无效，建立新的题目修订并重新生成相关回答，旧评分不继承；补足本轮有效题目数量，不能靠删除难题提高通过率。

### 8.3 追加纠正素材

继续使用“用户输入、已经发生的医生回答、期望医生回答”表单，下方展示完整素材库并支持分页。

本轮只补充来源、被哪些版本采用，以及“来自评审反馈”的关联信息，不恢复场景或沟通行为分类。当前已确认的 8 条素材继续保留，不因实施本设计再清空一次。

## 9. 发布与运行时边界

建议保持开发阶段清晰的发布条件：

- 冻结的有效人工验收题达到本轮约定数量，且全部完成人评并接受。
- 不存在未解决的关键语义问题、执行异常、兜底失败或已知关键回归失败。
- 题目错误和自动误判已复核，不能用平均分掩盖。
- 指引和示例产物未在评审后被覆盖。

三个主观评分用于定位问题和观察分布，初期不再增加一套未经验证的复杂加权总分门槛。发布资格仍以明确的人评结论和检查结果为准。

发布与启用继续分开，均需显式操作。“接受测试”“添加素材”“重新构建”都不能自动切换线上版本。

评测失败不影响当前已启用版本；Style Agent 在线运行仍执行内部语义检查和有界重试，不因某版本离线评测通过就省略这些步骤。

## 10. 数据和模块设计建议

### 10.1 在现有 feature 内调整，不恢复旧系统

继续以 `style_management/` 和 `agents/style/` 为边界，前端仍在 `frontend/src/components/style/`。不恢复旧 `style_ab_*` 表或旧路由，不另建同职责的兼容平台。

建议把构建器目前的大段职责拆开：

| 模块 | 职责 |
| --- | --- |
| `style_management/construction/` | 素材整理、Guide 构建、反馈归纳、实际产物差异 |
| `style_management/evaluation/` | 出题、题目校验、去重与污染检查、运行评测、汇总 |
| `style_management/worker.py` | 后台任务调度、租约、阶段与恢复 |
| `style_management/runtime.py` | 读取固定产物，向离线评测提供同一 Style Agent 调用入口 |
| `agents/style/` | 表达转换、检索、语义检查、重试和兜底；不感知人评页面 |

具体拆文件按实现规模决定，避免为每个名词新建框架。

### 10.2 最小数据契约

不再要求一个评审用例必须有 `example_id`。建议先复用现有版本及评审表，使用结构化快照承载测试集，避免开发期额外引入一套复杂任务库：

- `Version`：保留指引和示例快照；补充父版本、消费的反馈、实际变更清单、冻结测试集及评测配置。
- 测试集快照：题目 ID、题族 ID、修订号、来源、固定／新题／回归标签、相对当前构建的暴露情况、自然输入、上下文、中性稿、内容保护契约。
- `ReviewCase`：对应版本和测试题修订；保存实际输出、是否兜底、自动检查、人工评分、可选期望回答、拒绝理由及问题标记。
- `Example`：保留三字段素材；评审回流时保留来源用例，支持追踪该题何时不再独立。

测试题来源标签与“是否独立”分开存储：固定题也可能被污染，回归题也不一定有逐字相同的构建样例。

只有结果状态可以更新；冻结产物、已完成题目和被评分的输出不能静默替换。任务重试恢复未完成工作，不覆盖已经评分的回答。人工评分一旦绑定旧题目修订，不能自动继承到新题目。

### 10.3 API 与事务

- 保持 `/api/admin/styles` 主入口，扩展现有构建和评审契约，不增加独立 A/B API。
- 新构建参数可包含 `revision_of_version_id`、待消费反馈及人工验收数量。
- 对构建、反馈回流和重评使用幂等键；重新构建期间冻结输入，后续反馈留给再下一次。
- 版本必须属于当前风格；所有写请求沿用管理员认证及 CSRF 保护。
- 替换无效题时，同步失效其旧结果和评分；删除版本时清理依赖，不留下孤儿记录。
- 不改变 RAG、健康档案、聊天记录、个人记忆的数据职责。

## 11. 实施顺序与验收

### 11.1 实施顺序

1. 先拆开素材与测试用例契约，补全无副作用的题目生成和验证。
2. 将构建后的逐素材评审改为固定规模独立评测；保持 Style Agent 在线行为一致。
3. 调整人工评分、语义检查展示及无效题处理。
4. 补充“根据反馈重新构建”、反馈消费与题目污染标记、产物差异展示。
5. 完成 UI、迁移验证和实际模型验收，再按明确授权发布。

实施时保留现有素材，不把旧三项评分中的“语义忠实”数值转换成新“自然程度”分数。旧的素材型评审不能直接改名为独立测试。

对未启用候选，应在备份并确认范围后清理旧式评测结果，重新生成新契约下的测试；对已经启用的版本，不因本次设计自动停用或删除。实施前核查实际状态，不能根据本文假定线上仍只有基础版本。旧契约、旧字段和旧 UI 的替换要同步完成，避免长期运行两套评审体系。

### 11.2 必须通过的验收

- 8 条或 300 条素材都不会强制生成同数量人工评审；默认都是 12 条有效测试。
- 独立题没有抄取构建期望答案；近重复题不能靠改几个数字躲过去重。
- 用户已说明餐次时，不再生成要求确认同一餐次的矛盾中性稿。
- 中性稿错误与风格改写错误能区分；模拟记录不会写入真实业务数据。
- 用户输入不含场景说明前缀、模型分析或整理理由。
- 明确身份回答不会变成鼓励；数字、保存失败状态等保持一致。
- 没有示例命中时可以仅用 Guide；原文已合适时允许不改字。
- Style Agent 内部失败重试携带原因；兜底、失败、成功分别统计。
- 接受不必填理由，拒绝必填；语义错误不能被高平均分抵消。
- 新评分与旧评分不混用；无效题换题后不保留旧评分冒充验收。
- 反馈进入下一版后，题目及近重复题自动转为回归，补足新题。
- 重复提交不重复增加素材；接受测试默认不自动扩大素材库。
- “这版改了什么”与实际 Guide／示例差异一致，能够定位反馈来源。
- 重新构建不会修改已评审版本，也不会自动发布或启用。
- 素材只在完整示例库分页展示，不要求人工评审全部素材，也不新增第四个 Tab。

测试按 [AGENTS.md](AGENTS.md) 的定向策略执行；数据契约、后台任务和迁移改动需要覆盖相邻回归。真实模型验收用隔离的合成数据，不冒充真实用户会话。

## 12. 研究依据与适用边界

### 12.1 为什么保留多维评价

文本风格迁移研究将风格迁移程度、内容保持和自然性作为不同评价方面，并指出这些方面可能存在权衡。它支持分开评估，而不是只看“像不像”。本文的三个主观分及版本级统计是结合产品需求提出的设计，不是论文规定的统一表单。[Evaluating Style Transfer for Text，NAACL 2019](https://aclanthology.org/N19-1049/)

### 12.2 文字风格也有少样本迁移方法

TinyStyler 使用预训练作者风格表示和生成模型进行少样本风格迁移，说明可以研究专门的风格表示，而非仅依赖普通内容相似度。其结果不能直接证明我们当前的通用向量检索能衡量医生语气，更不能直接保证中文短对话的效果。[TinyStyler，EMNLP 2024](https://aclanthology.org/2024.findings-emnlp.781/)

另一项研究在其英文数据上发现，非正式表达中的个人风格模仿仍有明显局限，增加示例也未消除差距。对 SlimGuard 的启示是需要实际验收，不能把素材量当成风格可靠性的替代指标。[Catch Me If You Can? Not Yet，EMNLP 2025](https://aclanthology.org/2025.findings-emnlp.532/)

### 12.3 为什么不能只让模型按自己提炼的 Guide 打分

一篇 2026 年预印本在有限的英文博客与模型实验中报告了指引提炼和特征判分之间的循环性：满足模型提炼的特征，不必然等于模仿了作者。该研究不能外推成所有模型的定论，但支持我们保留人工判断、固定参考与多个诊断维度。[Theory-Grounded Evaluation Exposes the Authorship Gap in LLM Personalization](https://arxiv.org/abs/2604.26460)

### 12.4 与十几秒声音克隆的区别

VALL-E 论文先在约 6 万小时英语语音上预训练，再使用 3 秒参考录音作为新说话者的提示。短参考并不等于只用几秒数据从头训练。[Neural Codec Language Models are Zero-Shot Text to Speech Synthesizers](https://arxiv.org/abs/2301.02111)

对本项目的类比是“内容稿＋表达参考”，不是凭几句口头禅就能掌握一个人在所有语境中的语言选择。当前 8 条素材可以启动流程，但不能据此宣称已完整克隆真人风格。

本阶段继续采用 Guide＋少量表达示例，不立即引入模型微调、专用作者向量模型或新的外部服务。是否升级这些技术，应由固定测试及真实使用反馈证明必要性；不把升级技术当作跳过评测的理由。

## 13. 用户确认清单

本文建议优先确认四项产品选择：

1. 每轮默认 12 条，人工规模限制在 10～15 条，素材数量不影响待评审数量。
2. 人工三项为风格匹配、自然程度、交流分寸；语义忠实单独检查和复核。
3. 接受仅表示验收通过；明确加入素材或消费反馈后，该题在后续版本中转为回归题。
4. 不满意后点击“根据反馈重新构建”，新版本必须展示实际指引变化和同题回答变化。

确认后再进入实现。本设计文档本身不会更改现有 8 条素材、任何评分或线上风格。
