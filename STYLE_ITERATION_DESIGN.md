# 风格版本自助迭代设计

- 状态：Proposed
- 版本：v1
- 日期：2026-09-08

## 1. 背景

SlimGuard 已具备以下能力：

- 管理员可以在“风格纠正”中追加已脱敏场景、用户消息、当前回复和期望回复；
- 纠正记录及实名 A/B 评分均为 append-only，并带内容 Hash；
- 可以冻结某一版本的评审输入，离线生成 Style Profile、示例、合成场景和真实模型 A/B；
- 自动评估与人工 A/B 必须同时通过，候选资产才能发布；
- 已发布 Style Profile 可以通过数据库仓储激活，运行时每 Turn 冻结精确版本。

当前缺口是：从“素材已追加”到“候选版本可评审”仍依赖开发者执行多条命令并手工组织文件，发布和默认
版本切换也不在管理台内完成。新增场景虽然可以被记录，但尚无产品化流程保证它经过分类、抽象、生成和
回归评估。

本设计覆盖两个需求：

1. 管理员在页面点击“构建下一版本”，查看可审计的构建过程；自动评估通过后进入人工评审，整套采纳后
   发布为新版本，并可在页面中启用或回滚。
2. “风格纠正”允许追加基础 12 条以外的任意场景；构建下一版时，新场景必须被分类、抽象并形成回归
   Case，而不是只记住一句固定回复。

产品文案统一使用“风格迭代”或“构建下一版本”，不使用容易被理解为修改基础模型权重的“训练”。

## 2. 目标与非目标

### 2.1 目标

- 将素材冻结、候选生成、自动评估、A/B 导入、发布、启用和回滚串成一条可恢复的管理台工作流。
- 每次构建都有唯一版本、输入快照、模型信息、精确 Hash、事件历史和操作者，可完整复现和审计。
- 新增场景只要属于表达问题，就能成为新版本的规则输入和长期回归 Case。
- 自动化不越过现有安全边界：模型不能自行批准、发布或启用候选版本。
- 开发阶段支持人工全量采纳后直接启用；未来生产环境仍可要求 Canary。
- 构建失败、服务重启或重复点击时不产生半成品发布、重复 Case 或不可解释的运行状态。

### 2.2 非目标

- 不微调 GLM 或其他基础模型权重。
- 不把医生群聊、用户健康事实或期望回复中的专业建议写入 Style Profile。
- 不让 Style 层决定应该记录什么、诊断什么或提供什么营养方案。
- 不展示模型隐藏思维链。页面只展示可验证的阶段、输入摘要、结构化分类、产物、指标和失败原因。
- 不允许浏览器上传或指定一份任意 bundle 后直接发布。
- 第一版不自动修改 `CommunicationAct` 代码契约，也不自动编写业务代码。

## 3. 核心产品原则

### 3.1 “采纳”和“启用”必须分开

- **采纳并发布**：确认精确候选资产已经通过全部自动评估和实名人评，将其保存为不可变、可供运行时
  读取的 `evaluated` 版本。
- **启用**：把已经发布的精确版本设为当前默认运行版本。这是一次独立、可审计、可回滚的流量操作。

即使单人开发阶段由同一管理员完成两个动作，页面也保留两个按钮，避免评审结束时误切流。

### 3.2 候选在评审前就必须持久化

“采纳后保存”不能理解为采纳前不落库，否则 A/B 评审对象无法被精确绑定和复现。构建完成时先保存
不可变候选产物及其 Hash；采纳后再把这份精确产物提升为已发布 Style Profile。被拒绝的候选仍保留为
审计记录，版本号不复用，也不覆盖。

### 3.3 单条反馈不即时改变 Agent

追加反馈只进入素材池。只有新版本完成以下整条链路后，才可能影响运行时：

```text
素材冻结 → 场景分类 → 候选生成 → 回归生成 → 真实 A/B → 自动评估
                                                   ↓
                                            实名人工评审
                                              ↙       ↘
                                          拒绝         全部采纳
                                           ↓              ↓
                                      构建下一版       发布 → 启用
```

## 4. 用户体验

### 4.1 页面结构

在管理台增加“风格版本”页面，并保留现有“风格纠正”和“风格 A/B 人评”：

1. **当前运行版本**
   - 当前精确 Profile 版本、启用时间、操作者；
   - 上一个可回滚版本；
   - 环境默认回退版本；
   - “启用此版本”和“回滚”操作。
2. **素材概览**
   - 基础版本；
   - 已完成 A/B 数、接受数、拒绝数；
   - 尚未被某次构建消费的风格纠正数；
   - 新场景、上游逻辑问题和待人工处理项数量。
3. **构建任务**
   - “构建下一版本”按钮；
   - 系统分配的目标版本，例如 `doctor_strict_v4`；
   - 实时阶段、耗时、模型调用数、输入/Bundle/Eval Hash；
   - 失败原因、重试资格和后续动作。
4. **候选版本**
   - Profile 规则差异、禁止短语差异、示例差异；
   - 基础回归和新增场景回归清单；
   - 自动评估结果；
   - 进入现有 A/B 人评页的入口；
   - 全部人工接受后显示“采纳并发布”。
5. **版本历史**
   - `building`、`needs_action`、`failed`、`ready_for_review`、`rejected`、`evaluated`、`active`、
     `retired`；
   - 来源版本、输入 Hash、发布回执、启用/回滚历史。

### 4.2 点击“构建下一版本”前的页面校验

按钮只有在以下条件满足时可用：

- 操作者已通过管理员会话认证；
- 来源版本的 A/B Case 已全部完成人工评分；
- 至少存在一条拒绝评分或尚未消费的风格纠正；
- 当前 Profile 没有另一条进行中的构建任务；
- 服务端模型凭据和目标模型配置可用；
- 所有将发送给模型的反馈都已经确认脱敏且只用于表达学习。

页面在确认弹窗中显示来源版本、输入数量、预计生成 Case 数和预计模型调用范围。浏览器不接收模型密钥，
也不能自报 `actor` 或自行指定已存在的版本号。

### 4.3 构建过程展示

页面通过轮询任务与 append-only 事件列表展示以下阶段：

1. 校验来源版本；
2. 分配并保留下一版本号；
3. 冻结实名 A/B 与风格纠正，计算 `source_sha256`；
4. 对新增场景进行结构化分类；
5. 生成候选 Profile 规则与去事实化示例；
6. 生成基础回归和新增场景回归；
7. 使用同一模型生成基线与候选回复；
8. 执行风格、忠实度、隐私、无冒充、无辱骂和无新增专业结论评估；
9. 校验全部产物 Hash 并导入 A/B 人评；
10. 等待实名人工评审。

每个事件可以展示结构化摘要、数量、Hash、模型、耗时和错误码，但不展示模型隐藏思维链。包含脱敏反馈
正文的详情沿用管理员权限；公开日志只显示 ID、分类和 Hash。

## 5. 任意新增场景的处理

### 5.1 分类结果

每条新增纠正在构建时必须得到一个可审计分类：

| 分类 | 含义 | 构建动作 |
| --- | --- | --- |
| `style_existing_act` | 能归入现有六类表达行为 | 抽象语气规律，生成对应新回归 Case |
| `style_composite` | 一条回复包含多个已有行为 | 保留一个主行为，在 ResponsePlan 中明确全部内容块并生成回归 Case |
| `new_act_required` | 现有六类无法准确表达 | 暂停为 `needs_action`，提出契约扩展建议，不强行归类 |
| `upstream_logic` | 问题在日期、餐次、工具调用、事实判断或业务流程 | 不学习为风格；要求先修改上游逻辑和测试 |
| `professional_content` | 期望回复包含营养、诊疗或其他专业主张 | 隔离并要求专业知识/证据流程审核，不进入 Style 示例 |
| `privacy_or_identity` | 含未充分脱敏信息、真人身份模仿或私人关系 | 阻断该条输入并要求人工修订 |
| `invalid_or_conflicting` | 当前回复与期望相同，或与已批准规则直接冲突 | 要求人工消解冲突 |

分类模型只能给出结构化类别、理由摘要和证据字段引用。服务端再次按 Schema、隐私规则和事实边界校验，
不能只相信模型结论。

### 5.2 现有六类行为

当前 `CommunicationAct` 为：

- `acknowledge`：确认；
- `correct`：纠正；
- `remind`：提醒；
- `encourage`：鼓励；
- `explain`：解释；
- `ask`：询问。

大多数新场景应通过“新的场景内容 + 既有表达行为”进入系统，而不是不断增加行为枚举。例如“聚餐吃多了”
可以形成“确认偏离 + 简短下一步”的场景回归，但不会把“聚餐”本身写成全局语气规则。

如果确实出现无法归入六类的新行为，第一版构建器不得自动修改 Pydantic/数据库契约。任务进入
`needs_action`，页面显示建议的新行为、受影响场景及需要的代码和测试变更；完成契约升级后再启动新构建。

### 5.3 从单条纠正到可泛化风格

对可以进入 Style 的新增场景，构建器同时产出三类内容：

1. **场景事实层**：只保留合成后的用户处境、系统已确认上下文和回复目标。
2. **表达抽象层**：归纳篇幅、直接程度、句式、是否追问、应避免的套话等，不包含食物、日期、体重或
   医学结论。
3. **回归层**：把新场景绑定到精确 ResponsePlan，生成基线/候选输出并验证受保护事实。

例如：

```text
用户：今天聚餐吃多了
当前：没关系，偶尔一次不用担心，继续加油！
期望：吃多了就记下来，明天按计划来。
```

可抽象为“面对偏离计划，直接确认并给出上游已经确定的下一步，不追加泛泛安慰和口号”。“明天按计划来”
只有在 ResponsePlan 已确认该行动时才能输出；Style 不能自行创造行动建议。

### 5.4 回归集策略

每次候选评估至少包含：

- 固定的基础 12 条：六类行为各一个社交表达 Case 和一个受保护事实 Case；
- 本次新消费的每一条有效纠正所派生的 Case；
- 以前版本中由真实纠正产生、尚未显式退休的独特回归 Case；
- 上游逻辑修改涉及的端到端 Case。

语义重复的反馈可以聚合到同一规则，但来源 ID/Hash 不能丢失。已经通过的真实失败回归默认长期保留；
若未来因成本需要退休，必须由管理员显式操作并记录原因，不能由模型静默删除。

## 6. 构建任务与状态机

### 6.1 为什么使用后台任务

真实生成与 Judge 可能持续数分钟，不应占用一个长 HTTP 请求。`POST` 只创建持久化任务并返回 `202`；
后台 Worker 从数据库领取任务、更新租约和阶段，页面轮询任务状态。服务重启后，过期租约可以被安全接管。

第一版不要求引入外部队列，但任务、事件、租约和幂等键必须落在 PostgreSQL，不能只存在进程内存中。

### 6.2 任务状态

```text
queued
  → validating
  → freezing_inputs
  → classifying
  → building_profile
  → generating_cases
  → evaluating
  → importing_review
  → ready_for_review
  → evaluated
  → active
```

旁路状态：

- `needs_action`：新行为、上游逻辑、隐私或专业内容需要人工处理；
- `failed_transient`：供应商超时、限流等，可从安全阶段重试；
- `failed_terminal`：Schema、事实保护或完整性失败，需要构建下一候选；
- `rejected`：人工评审至少一条拒绝，不能发布；
- `cancelled`：只停止后续步骤，已完成事件和产物保留。

同一 Profile 同时只允许一条非终态任务。双击按钮使用客户端幂等键返回同一任务。临时重试保留原始尝试
事件；只要候选规则、示例或 Case 内容发生变化，就分配新版本，绝不覆盖原版本。

### 6.3 建议新增的数据实体

#### `style_iteration_runs`

- `id`、`profile_id`；
- `source_version`、`target_version`；
- `status`、`stage`、`progress_current`、`progress_total`；
- `source_sha256`、`profile_sha256`、`bundle_sha256`、`cases_sha256`、`evaluation_sha256`；
- `generation_model`、`judge_model`；
- `created_by`、`created_at`、`started_at`、`completed_at`；
- `lease_owner`、`lease_expires_at`、`heartbeat_at`；
- `failure_code`、`failure_summary`；
- `published_version_record_id`。

#### `style_iteration_inputs`

- `run_id`；
- `source_kind`：`ab_review` 或 `style_feedback`；
- `source_id`、`source_sha256`；
- `classification`、`classification_summary`；
- `derived_case_ids`；
- 唯一约束 `(run_id, source_kind, source_id)`。

该表保存冻结引用；原始内容仍在现有 append-only 来源表中。构建快照对标准化内容再次整体计算
`source_sha256`。

#### `style_iteration_events`

- `run_id`、单调递增 `sequence`；
- `event_type`、`stage`、`status`；
- `public_summary`；
- 有界、脱敏的 `technical_metadata_json`；
- `created_at`。

事件只追加，不更新或删除。

#### `style_activation_events`

- `profile_id`；
- `previous_version`、`activated_version`；
- `actor`、`reason`、`created_at`；
- `runtime_revision`。

激活指针本身可以更新，但每次变化都必须对应一条不可变事件。

## 7. 服务边界

现有 CLI 中的业务逻辑应提取为可复用服务，CLI 与后台 Worker 调用同一服务；Web 进程不得通过拼接 Shell
命令驱动构建。

建议拆分为：

- `StyleIterationService`：校验、版本分配、输入冻结、状态迁移；
- `StyleFeedbackClassifier`：对新增反馈做结构化分类；
- `StyleProfileBuilder`：生成 Profile 差异和去事实化示例；
- `StyleRegressionBuilder`：构建基础、历史和新增回归 Case；
- `StyleEvaluationRunner`：真实生成、自动 Judge 和完整性校验；
- `StylePublicationService`：检查精确 A/B 批准回执并发布；
- `StyleActivationService`：切换已发布版本并记录回滚点。

每一阶段只接收上一阶段冻结的 Artifact ID/Hash，不能接收浏览器临时传入的正文来绕过素材与审核记录。

## 8. 管理 API 草案

所有写接口要求管理员会话、CSRF、服务端 actor、审计事件和限流。

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| `GET` | `/api/admin/style-iterations/context` | 当前运行版本、可构建来源及素材统计 |
| `POST` | `/api/admin/style-iterations` | 创建下一版本构建任务，返回 `202` |
| `GET` | `/api/admin/style-iterations` | 查询历史任务 |
| `GET` | `/api/admin/style-iterations/{run_id}` | 查询阶段、产物和门槛 |
| `GET` | `/api/admin/style-iterations/{run_id}/events` | 增量读取构建事件 |
| `POST` | `/api/admin/style-iterations/{run_id}/retry` | 只重试允许重试的临时失败 |
| `POST` | `/api/admin/style-iterations/{run_id}/cancel` | 停止尚未完成的后续步骤 |
| `POST` | `/api/admin/style-iterations/{run_id}/publish` | 在精确 A/B 全部接受后发布候选 |
| `POST` | `/api/admin/style-runtime/activate` | 启用一个已发布精确版本 |
| `POST` | `/api/admin/style-runtime/rollback` | 回到上一已发布版本 |

创建任务请求只包含 `source_profile_version`、客户端幂等键和明确确认；`target_version` 由服务端在事务中
分配。发布接口不接受 Profile、Bundle 或评估正文，只根据 `run_id` 读取服务端冻结的精确产物。

## 9. 发布、启用与运行时解析

### 9.1 发布门槛

“采纳并发布”按钮只有在以下条件全部满足时可用：

- 构建任务为 `ready_for_review`；
- 基线和候选均为真实模型生成且没有降级；
- 自动评估全部通过；
- 人工评分覆盖精确 Case 集，且每条最新决定均为 `accept`；
- Bundle、Evaluation、Case、场景和 ResponsePlan Hash 与构建任务一致；
- Profile 仍是 `expression_only`，没有隐私、真人冒充或新增专业结论；
- 发布操作者再次明确确认。

发布后写入现有 Style Profile 仓储，状态为 `evaluated`。发布不自动启用。

### 9.2 页面启用

Web 服务不修改 `.env`，也不从页面执行进程重启。新增一个数据库控制的、带单调 revision 的运行时版本
指针；管理员点击启用时原子更新指针并追加激活事件。

每个 Turn 开始时解析并冻结精确版本，Turn 中的 Style、Reviewer 和 repair 都复用同一快照。运行时可以
短时缓存控制记录，但必须按 revision 失效。目标资产读取或完整性校验失败时，回退到环境变量指定的
`DEFAULT_STYLE_PROFILE`，并产生可观察告警。

开发环境允许从 `evaluated` 直接全量启用。生产环境默认禁止页面直接全量，必须按既有 Shadow/Canary
策略放量。环境变量继续作为启动兜底和紧急回退手段，不作为管理台正常切换版本的实现方式。

### 9.3 回滚

回滚只移动运行时指针，不删除版本、评分、反馈、构建事件或发布回执。页面默认提供“回到上一版本”，
也可以从已发布历史中选择精确版本；草稿、失败、拒绝或退休版本不能启用。

## 10. 安全、隐私与成本控制

- 继续要求 `deidentified_confirmed=true` 和 `expression_only_confirmed=true`。
- 原始微信群 HTML、图片、语音、用户 Memory 和营养知识库不进入构建任务。
- 模型只看到完成脱敏且本阶段必需的最小内容；专业内容分类后不送 Style Profile Builder。
- 所有模型输出都经过严格 Schema 校验、事实块一致性校验和违禁内容检查。
- 页面不显示供应商密钥、原始 Request/Response 或隐藏思维链。
- 操作者只能来自服务端登录会话；客户端不能指定审核人。
- 构建前显示预计 Case 和模型调用量；服务端设置每任务最大 Case 数、最大调用数、超时和 Token 预算。
- 达到预算或发生降级时任务失败，不得用降级文本冒充完成的候选。
- 发布、启用、回滚为不同审计事件。未来多管理员环境可进一步拆成 builder、reviewer、publisher、
  activator 权限；当前单管理员仍保留动作边界。

## 11. 失败处理

| 失败 | 系统行为 |
| --- | --- |
| 来源 A/B 未评完 | 不创建任务，返回待评数量和入口 |
| 没有新素材 | 不创建空版本 |
| 模型超时/限流 | `failed_transient`，允许幂等重试 |
| Profile/Case Schema 非法 | `failed_terminal`，保存错误摘要，不导入 A/B |
| 受保护事实被改写 | 自动评估失败，不导入 A/B |
| 新行为无法归类 | `needs_action`，要求契约设计，不自动扩枚举 |
| 上游业务逻辑问题 | `needs_action`，指出受影响 Agent/工具规则 |
| 隐私或专业内容风险 | 隔离该输入，要求人工修订 |
| A/B 有任一拒绝 | 候选标记 `rejected`，拒绝批注进入下一版素材 |
| 发布时评分已被更正 | Hash/最新 review 不一致，发布失败并要求重新核对 |
| 启用时资产损坏或未发布 | 拒绝切换，保持当前版本 |
| 激活后运行时解析失败 | 回退环境默认版本并告警，可一键回滚 |

任务不能通过删除失败记录“重新开始”。新的实质性候选使用新版本号；临时重试则在同一任务下追加 attempt
与事件。

## 12. 可观测性

至少记录以下指标：

- 构建成功率、各阶段耗时、重试率和失败码；
- 每次构建的反馈数、派生 Case 数、模型调用数和 Token；
- 新场景各分类占比；
- 自动评估通过率、人工接受率和从素材到发布的周期；
- 发布后按 Profile 版本统计 Reviewer 拒绝率、降级率和真实风格纠正新增率；
- 激活、回滚及运行时版本回退次数。

Trace 只记录结构化可验证信息，不记录隐藏思维链。

## 13. 测试与验收标准

### 13.1 后端

- 同一幂等键或双击只创建一个任务；同一 Profile 不能并行构建两个版本。
- 版本号由服务端唯一分配，失败或拒绝版本不会被覆盖或复用。
- 服务重启后 Worker 能接管过期任务，已完成阶段不重复导入。
- 冻结快照稳定：相同来源得到相同 `source_sha256`，来源更正后得到新 Hash。
- 任意一个基础 12 条以外、可归入现有行为的反馈都会产生绑定来源 ID/Hash 的回归 Case。
- 新场景中的食物、数字和健康事实不会被写成全局 Style 规则。
- `upstream_logic`、`professional_content` 和隐私风险不会进入 Style 示例。
- 新行为无法归类时任务停在 `needs_action`，不会伪造第七类行为。
- 任一真实生成降级、自动 Judge 失败或受保护事实变化都会阻止 A/B 导入。
- 任一最新人工决定为拒绝都会阻止发布。
- 发布只接受任务中冻结的精确 Bundle/Evaluation/Case Hash。
- 只有已发布版本可以启用；启用和回滚均写 append-only 审计事件。
- 一个 Turn 内发生版本切换时，进行中的 Turn 仍使用开始时冻结的版本。

### 13.2 前端

- 构建按钮准确解释禁用原因和待完成动作。
- 页面刷新后仍能恢复正在执行任务的阶段和事件。
- 构建过程区分结构化摘要与敏感详情，不显示隐藏思维链或密钥。
- 候选页面明确区分自动评估与实名人工评审。
- 有拒绝时不显示可用的发布按钮；发布后也不会自动启用。
- 启用弹窗显示当前版本、目标版本和回滚版本。
- 小屏幕下任务进度、Profile 差异和 A/B 入口可完整使用。

### 13.3 端到端验收场景

1. 追加一个不在基础 12 条中的表达纠正。
2. 点击“构建下一版本”，页面显示输入冻结和分类结果。
3. 新场景出现在候选回归 Case 中，基础 12 条仍全部存在。
4. 自动评估通过后进入现有 A/B 页面，自动结果不等于人工通过。
5. 模拟一条拒绝，确认发布被阻断且批注进入下一版来源。
6. 将精确 Case 全部接受，点击“采纳并发布”，确认运行版本仍未改变。
7. 点击“启用此版本”，新 Turn 使用新版本，旧 Turn 保持原快照。
8. 点击回滚，后续 Turn 回到上一已发布版本，历史数据完整保留。

## 14. 分阶段实施建议

### Phase A：持久化构建控制面

- 新增 iteration run/input/event 表和仓储；
- 把 `prepare_style_iteration` 的核心逻辑提取为服务；
- 实现版本分配、输入冻结、幂等和任务租约；
- 先提供只读任务页和构建进度。

### Phase B：新场景分类与自动候选

- 实现结构化分类器及二次服务端校验；
- 复用现有 Profile Builder、真实生成和 Judge；
- 固定基础 12 条，并把有效新场景加入累计回归集；
- 自动导入通过的精确候选到现有 A/B 人评。

### Phase C：页面发布与启用

- 增加版本级发布门槛和按钮；
- 增加数据库运行时版本指针、激活事件和回滚；
- 运行时按 Turn 冻结版本，环境变量作为故障回退；
- 开发环境允许全量启用，生产环境保留 Canary 限制。

### Phase D：恢复、成本和安全加固

- 服务重启接管、临时重试、取消和预算上限；
- 完整的权限、CSRF、并发、错误注入和隐私测试；
- 构建、发布、激活及线上质量指标。

## 15. 现有 v3 的接入

`doctor_strict_v3` 已有冻结输入、候选 Profile、Bundle、回归 Case、真实 A/B、自动评估和数据库中的
12 条待人工 Case。实施本设计时不应重新调用模型或制造一套不同的 v3；应创建一次只读的历史构建任务，
引用现有精确 Hash，并将状态设为 `ready_for_review`。

用户完成 v3 人评后：

- 任一拒绝：v3 保持被拒绝审计状态，拒绝批注和额外风格纠正共同进入 v4；
- 全部接受：页面允许将精确 v3 采纳并发布，再由用户单独点击启用；
- 未点击启用前，当前运行版本保持不变。

## 16. 最终决策

该需求可行且值得实施。推荐建设的是“有人工门槛的自助风格版本流水线”，而不是“追加一条反馈就自动
学习并上线”。它把现有手工能力产品化，同时保留可复现、可审核、可回滚和不迁移专业内容的边界。
