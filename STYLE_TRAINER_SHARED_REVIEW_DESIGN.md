# 风格训练器、Style Agent 与共享审查组件设计

状态：主体已按用户后续授权实现于本地代码，尚未部署；具体操作、迁移范围及未完成细项见 [实现手册](STYLE_MANAGEMENT.md)。日期：2026-09-18。

本文件保留目标设计与决策依据，不用设计条目替代实际验收。当前未执行真实模型构建、线上数据清理或服务器部署。单题纠错后局部重评和真实模型审查校准仍需后续补充。

## 1. 结论与文档关系

将表达风格系统分为三项独立职责：

1. **风格训练器（Style Trainer）**：处理素材、首次构建、纠正后重建、自动优化和版本评测。
2. **Style Agent**：读取冻结的风格版本，改写本轮原回答，负责审查后的修复和兜底。
3. **共享回复审查组件（Style Review）**：对单次改写给出语义及表达判决，供训练和线上共同使用；本身不修改回答。

Style Agent 不调用训练器。训练器通过同一 Style Agent 批量运行评测，Style Agent 内部调用共享审查组件。不需要把三个模块拆成三个独立部署的服务。

本阶段的“训练”是自动构建、选择和优化表达指引及示例，不更新基础模型权重。不引入小模型微调，不在每次聊天时动态检索表达示例，也不恢复用户意图分类和话术模板路由。

本文以本轮讨论结论为主要依据，不是对旧评测文档的照搬或增补。优先级为：用户最新明确要求与讨论共识 → 本文中的目标设计 → 历史文档参考。本文补充的实现建议不冒充已经逐项确认的需求。

- [独立评测旧草案](STYLE_EVALUATION_ITERATION_DESIGN.md)：**仅作历史参考，不作为本轮需求或验收依据**；其中的配额、评分替换、动态选例等建议不自动继承。有冲突时以本轮讨论及本文为准。
- [原风格重构设计](STYLE_PROFILE_REFACTOR_DESIGN.md)：用于理解既有系统背景，不覆盖本轮已经调整的构建、使用和审查方案。
- [UI 设计及效果图](STYLE_UI_DESIGN.md)：沿用已讨论的布局和三子 Tab；字段、素材库两 Tab 和本轮交互以本文为准。
- [现有操作手册](STYLE_MANAGEMENT.md)：描述当前实现，不能把本文目标误认为已经上线。

## 2. 已明确的产品边界

- 默认使用医生风格，风格及版本按 style_id 隔离；未来用户选风格不在本次实现范围。
- 用户负责提供表达素材、纠正意见、最终人评；不负责选择算法、手动试 Prompt 或调示例数量。
- 追加素材目前纯人工手写，不做 Trace 导入、自动带入或后台抓取真实聊天。
- 完整素材库分“未参与构建”“已参与构建”两个 Tab，均支持分页。
- 每次构建冻结这两个 Tab 的全部现存素材，统一分析筛选；参与构建不等于被采纳。
- 支持正面表达、负面表达和文字纠正意见，人工输入也必须经过质量检查。
- Guide 与示例有共同证据基础、相互校验，不能分别生成后直接拼接。
- 示例数量不是固定 8 组；在资源预算内自动选择，版本完成后固定内容和顺序。
- 追加素材不触发自动构建、不影响线上；用户点击构建才启动任务。
- 自动优化的中间候选不逐个要求人评；最终候选提供约 10～15 条实际输出供验收。
- 接受不强制填写理由；拒绝必须填写理由。发布与启用仍为明确的独立操作。
- 独立审查可单独优化和版本化，但训练器不能通过放宽审查标准使自己“过关”。
- 训练过程必须清晰可见：管理员能知道正在做什么、处理进度、产生的结果、调整依据和等待／失败原因；不是只显示转圈、百分比或原始日志。

### 2.1 本文补充的实现建议

为使方案可实现，本文进一步建议：支持仅填写负面纠正意见而不提供期望回答；按素材修订计算参与状态；默认约 12 条最终人评；每次最多一次改写修复；独立版本化审查策略；采用第 13 节的目录和数据结构。以上是具体落地建议，不声称用户已经指定了这些字段约束、默认数值或技术名称。

用户已确认的方向——自动化训练、全库参与筛选、固定版本产物、共享审查、原因驱动重试、简洁人评及素材库两个 Tab——不因这些工程细节调整而改变。

## 3. 现状与目标的差异

本节描述重构前检查得到的差异，用于保留变更依据；不代表重构后的本地代码或实时服务器状态。

| 现有位置 | 当前行为 | 目标调整 |
| --- | --- | --- |
| style_management/worker.py | 批量分析素材、汇总 Guide，对每条可用素材创建人评用例 | 薄任务入口调用独立训练器；自动优化，最终候选才创建固定规模验收 |
| style_management/runtime.py | 解析版本，并提供基于原素材的 evaluate_example | 只做版本读取适配；评测执行迁入训练器，题目不再绑定原素材 |
| agents/style/agent.py | 改写、确定性检查、内嵌 SemanticCheck 模型调用及一次修复 | 保留改写闭环；审查逻辑提取为共享组件，返回结构化语义与表达判决 |
| agents/style/retrieval.py | 按原文词法／向量检索示例 | 下一版线上不调用；删除无调用方的风格专用检索实现 |
| agents/style/contracts.py | 风格上下文只允许少量动态示例，缺少明确用户消息和最小会话语境 | 改为冻结风格包＋动态改写请求；按 Token 预算而非历史示例数量限制 |
| expression_examples | 三字段素材，期望回答必填 | 增加纠正意见和修订标识，支持纯负面反馈 |
| expression_review_cases | example_id 必填，逐素材生成 | 关联独立测试用例及修订，不要求存在素材 ID |

风格向量检索退出不等于删除营养 RAG：营养库、其向量模型、COS、语料发布及检索链路完全不在本次替换范围。

## 4. 架构与依赖

```text
后台：构建版本
        │
        ▼
Style Trainer
├─ 冻结全库素材与反馈
├─ 审查素材、提取表达规律及证据
├─ 联合构建 Guide＋固定示例包
├─ 审查版本一致性
├─ 在预算内优化、比较版本
└─ 批量调用同一 Style Agent ────────────┐
                                     │
线上：已放行的原回答＋必要上下文 ───────┤
                                     ▼
                                 Style Agent
                                 ├─ 按固定风格包改写
                                 ├─ 调用共享 Style Review
                                 └─ 根据问题修复／兜底
                                            │
                                            ▼
                                      Style Review
                                      ├─ 确定性完整性检查
                                      ├─ 语义保真检查
                                      └─ 风格与表达检查
```

### 4.1 审查、评测和人评不是同一职责

| 能力 | 输入与输出 | 归属 |
| --- | --- | --- |
| 素材审查 | 原始素材 → 可用性、角色、冲突和理由 | 训练器 |
| 产物一致性审查 | 规则、证据、示例 → 无依据规则和矛盾项 | 训练器 |
| 单条回复审查 | 原稿、改写、内容约束、风格包 → 结构化判决 | 共享组件 |
| 版本评测 | 多条真实执行结果及对照 → 改善、退步、异常和成本报告 | 训练器 |
| 人工验收 | 最终候选实际回答 → 评分、接受／拒绝及反馈 | 管理台 |

共享的是单条回复审查，不是把完整训练器变成线上审核服务。公共数据结构和模型网关可以复用，但不能让线上读取素材 ORM、测试答案或训练任务状态。

## 5. 素材管理与参与状态

### 5.1 人工表单

| 字段 | 要求 |
| --- | --- |
| 用户输入 | 必填，按自然消息填写；必要前情可在输入或意见中说明 |
| 系统实际回答 | 必填，保留已经发生的输出 |
| 期望医生回答 | 可选，与纠正意见至少填写一项 |
| 纠正意见 | 可选，与期望回答至少填写一项；可描述希望保留或避免的表达 |

期望回答为空时，不自动伪造一条“人工期望”。系统可以据意见提出规则候选或建立负面检查，但这些都是机器推导，必须标明来源。

当前不增加自动导入入口，不从 Trace 拉取前情或改写前原文。人工材料缺少判断所需的信息时，输出“缺少上下文”，不能猜测用户事实。

### 5.2 两个素材库 Tab

- **未参与构建**：当前素材修订尚未出现在成功完成分析和产物报告的构建中。
- **已参与构建**：当前素材修订已被这样的构建处理，可能用于规则、用于示例、用于负面约束、被排除或暂待确认。

每条素材展示最近构建的处理结果、理由、关联版本，不能用“已参与”冒充“已采用／已上线”。一次构建完整结束但结论为“无提升”，仍可计为已参与；排队中、运行中、失败或取消不提前改变状态。

修改素材形成新的 revision；新修订回到“未参与构建”。已生成版本引用原修订的冻结副本，不随素材修改。参与状态由成功构建与修订的关联计算，不靠一个永久布尔值。

### 5.3 全库参与与质量筛选

每次冻结该风格两个 Tab 的全部现存素材，包括上次自动排除或待确认的素材。过去的判决是参考，不是永久黑名单。明确删除的素材不再进入后续快照，已有版本引用仍完整保留。

先区分素材的用途，再判质量：

- 正面表达对：语义一致的原回答与期望回答，可作为示例候选。
- 负面表达／意见：帮助形成有边界的禁止事项和回归检查，不作为目标回答模仿。
- 内容纠正：新增专业建议、事实或改变业务结论，不能直接训练纯风格转换。
- 语义漂移故障：原输出已经答非所问，可记录为失败约束；没有可靠中性原稿时，不伪装成等义表达对。
- 重复、冲突、上下文缺失、不可用：分别说明处理，不无声丢弃。

例如“怎么称呼你／不错，要保持／叫我 SlimGuard 就行”不能直接当成等义改写对。可明确标记“鼓励替代回答”的故障；只有后续评测拥有可靠的身份原稿时，才能检验保真修复。

素材数量不是证据强度：重复导入不增加规则可信度；单条明确意见可支持局部修正，但不能自动推导为普遍习惯。未选入线上示例的合格素材仍可支持规则和边界。

## 6. Guide 与固定示例包联合构建

### 6.1 先建立有证据的表达规律

大模型负责逐条分析差异及提出规律，程序核对引用 ID、原文片段和结构合法性，再汇总支持证据及反例。不能仅根据模型自报置信度决定规则可靠。

每条风格规则至少保存：规则 ID、表达倾向、适用边界、支持素材修订及片段、反例／冲突素材、推导理由、处理状态。

同时区分两类约束：

1. 从素材归纳的风格规则，例如减少无信息的铺垫。
2. 系统不可放宽的共同约束，例如不改变事实、状态、风险和身份。

后者不冒充医生个人特点，不允许素材或优化模型覆盖。判断上下文只为保留含义和把握分寸，不恢复用户意图识别或沟通行为路由。

### 6.2 选择示例，而不是先规定数量

从合格的表达对中选出相互补充的候选，覆盖已获支持的措辞、句式、节奏和适用边界。每个示例标明支持哪些规则、展示哪些边界，允许一例支持多条规则。

不因同属午餐话题就优先入选，也不把普通语义向量当成风格向量。去重和统计可以辅助选择，但不能证明对新输入有效。

在开发评测集上评估 Guide 和示例组合，执行有界的添加、移除、替换以及规则收窄。一次调整记录具体差异，效果接近时优先更短、更简单的组合。示例数量和顺序是构建结果，不是预设的“8 组最佳”。

资源上限与质量标准分开：提示 Token、调用次数、候选数量、总时限属于工程预算，不是理论最优值。达到预算后只能报告预算内的结果，不能宣称找到了全局最佳。

### 6.3 联合一致性审查

- Guide 概括过头：收窄或删除规则，不强行排除所有反例。
- 示例不可靠：移出正面示例包，保留素材和理由。
- 不同条件下均合理：明确边界，选取必要的边界示例。
- 同条件下偏好相反且无法判断：保留冲突，不捏造一致意见。

不能“先生成错误 Guide，再只挑支持它的素材”。每次改 Guide 都复核示例，每次换示例也复核整包表现。影响使用的未解决冲突阻止推荐；不相关、已明确隔离的坏素材不要求管理员逐条处理后才能构建。

### 6.4 版本产物

冻结的 StylePackage 包括：

- style_id、version_id、内容 Hash、协议版本；
- Guide 及其证据、边界和约束；
- 固定正面示例、必要的少量负面约束、固定顺序；
- 构建素材及修订清单、筛选结果引用；
- 根据结构化内容编译的固定风格 Prompt；
- 编译器／基础模板版本、实际执行配置签名；
- 构建报告、评测报告及实际产物差异的引用。

结构化 Guide／示例是可解释来源；编译 Prompt 是派生产物，不能两边独立编辑。相同输入及编译器版本应得到同一编译结果。超过预算需要构建失败或重新选择，不能线上静默截断示例。

## 7. 独立风格训练器

### 7.1 首次和纠正后构建共用流程

```text
人工点击构建
  → 冻结全库修订、反馈、当前启用版及执行配置
  → 素材质量检查、差异提取、证据与冲突整理
  → 构建初始 Guide＋示例包
  → 一致性审查
  → 调用 Style Agent 运行开发评测及历史回归
  → 在预算内根据具体问题调整组合，保留较好候选
  → 选定一个候选并冻结
  → 在最终验收集上与对照版同题比较
  → 输出结论、实际变化、问题和消耗
  → 最终人评；发布和启用需另行确认
```

首次构建以基础表达版本作对照；后续默认以点击构建时的启用版本作对照。针对被拒绝候选重建时，可以继承它的有效工作并加入反馈，但不能把该候选误称为线上版本。

### 7.2 小批纠正与缓存

只增加 1～2 条也可构建，意义在于解决具体问题，不保证每次都会改进。全部素材进入快照和整体决策；未变素材的分析可以缓存，键至少包含素材修订、分析器版本和模型配置。

增加意见可能改变旧素材的适用判断，不能复用上次筛选结论而跳过全局冲突分析。新材料不会在构建中途混入正在运行的任务。

### 7.3 有界自动优化

训练器可修改 Guide 和示例组合，不能修改审查器、评测标准、测试答案或原稿来抬高分数。

使用固定开发集和对照条件，在预算内提出、验证、选择候选。关键语义错误优先处理，不能用风格高分抵消。涉及收益很小或裁判分歧时保守处理，必要时对有限边界样本复核；不能挑一次随机高分当可靠提升。

必须记录并执行最大优化轮次、候选数、并发、模型调用数、Token、时限和单次请求上限。无改善、预算耗尽、无可用素材、待确认冲突、运行异常分别报告，不无限“修到通过”。

训练器保留现有 worker 的租约、所有权令牌、阶段恢复和幂等性。失败重试复用同一快照并恢复未完成阶段，不覆盖已评分回答、不重复回流素材。

### 7.4 比较结论

结论为“建议采用”“无明确提升”“不建议采用”“需要人工确认”，并说明依据。完成任务不等于推荐或发布。

报告至少展示：基线与候选 ID、执行配置、有效题数和异常数、同题更好／持平／更差、各维度分布、语义问题、旧故障修复与新增退步、修复／兜底次数、耗时及 Token。

“这版改了什么”从真实 Guide、示例、Prompt 差异计算，再生成解释。关联意见明确显示采纳、部分采纳、未采纳和理由；产物没有变化就明确报告，不靠模型的一句“已改进”作证。

## 8. 构建集、开发评测集与最终验收集

| 数据 | 用途 | 能否驱动当前构建优化 |
| --- | --- | --- |
| 构建素材及指定反馈 | 提炼规律、选择示例 | 可以 |
| 开发评测集，含回归题 | 比较组合、定位问题、选择候选 | 可以，但不再宣称是完全未见题 |
| 最终验收集 | 冻结候选后报告效果、生成最终人评 | 不可以 |

不机械将原始素材按固定比例切开。评测题可独立生成，包含自然输入、最小上下文、已核对的中性回答和内容保护契约；不执行真实业务工具、RAG 写入、记忆写入或消息发送。

造题不能读取当前候选 Guide 和期望答案来定制容易通过的题。题目先冻结，再运行输出；照片描述只代表合成条件，不假装实际调用过视觉模型。题目与原稿矛盾时先修题，不能用坏题处罚风格或让 Style Agent 补造业务事实。

### 8.1 最终验收与防泄漏

- 最终人评约 10～15 条；建议默认 12 条，仅是人工负担设置，不是统计可靠性的保证。
- 最终未见题与附加历史回归分别报告；不沿用旧草案把回归混入“独立验收”的固定 6／3／3 配额。
- 同题族、近重复和只换数字的题不能自动算未见题；开发集与最终集按题族隔离。
- 最终验收题库、参考答案和评分对候选构建／优化模块不可见。评测运行时仅将当前题的用户输入、最小上下文、中性原稿和内容契约传给 Style Agent，不传参考风格回答、评分或其他题。检查器只看到完成单条检查所需的数据，测试参考仅限离线评价。
- 验收结果一旦影响后续规则、示例或候选选择，该题及相关题族转为开发／回归用途，下次独立验收补入新题。
- 不先对多个候选反复跑最终集再挑最高分；先在开发集选定候选。
- 接受人评不默认把生成回答加入正面素材。自动结果不是新的真人风格证据。

### 8.2 同题比较与裁判依据

对照版和候选使用同一套输入、原稿、保护条件、模型配置和审查策略。历史对照需要按本次执行配置重跑时，标记“重新评测”，不能把它冒充旧历史结果。

线上每次检查关注保真和本风格的表达边界；离线“更像目标医生”还必须依据固定评分说明和用户认可的表达参考，不能仅以候选 Guide 自评。可做匿名、换序的同题比较，并使用已有人工结论检查裁判是否偏离目标。

独立调用不等于独立模型；同模型有相关偏差，需如实记录。拒绝解释和纠正答案可用于下一轮，不进入本轮改写提示。自动评分不替代最终人评。

## 9. Style Agent 运行时

### 9.1 固定 Prompt 与动态数据

固定：通用改写任务、不可放宽约束、冻结 Guide、固定示例及顺序、输出格式。

动态：用户当前输入、必要上下文、已通过上游检查的原回答、ResponsePlan 或等价内容契约、本次修复原因。

程序按固定模板组装请求，不让大模型每轮再编写一个风格 Prompt。版本不读取原始素材库，不执行风格向量查询；不同话题也使用同一冻结示例包。

动态上下文为消除指代和把握分寸服务，不用于硬编码“如果问号就询问”等分类。示例、原文和用户文本作为数据隔离，不得执行其中的指令，不迁移其他用户事实、专业观点或真人身份。

### 9.2 执行闭环

1. 本轮开始锁定已启用 StylePackage、实际模型／模板／审查版本及预算。
2. 校验原稿与内容保护契约有效；正常回复的业务内容已由上游确定。
3. 生成改写，调用共享审查组件。
4. 通过则返回；需修复则带原稿、失败输出和具体问题重写一次。
5. 仍未通过、审查不可用或预算不足，返回已获上游放行的中性原稿，并标记兜底原因。

最多两次生成；审查调用和重试均计入同一个 Agent／Turn 预算，预留审查额度，不能把未经审查的结果当成功。无依据不新增建议、鼓励、责备或行动；原文已合适时允许保持不变。

共享审查若发现输入契约自身无效，交还上游错误处理，不能把未放行原稿称为安全兜底。Style Agent 不纠正营养事实、不运行业务工具，也不触发素材构建。

## 10. 共享回复审查组件

### 10.1 单条输入与输出契约

以下是概念接口，不是已实现代码；具体实现复用项目已有模型网关、InvocationRunner 和结构化类型。

```python
class RewriteReviewRequest:
    source_text: str
    rewritten_text: str
    user_input: str
    minimal_context: dict
    content_contract: ContentContract
    style_package: StylePackage
    policy_version: str


class ReviewIssue:
    code: str
    dimension: str       # integrity / fidelity / expression
    severity: str        # blocking / repairable / warning
    source_excerpt: str | None
    output_excerpt: str | None
    explanation: str    # 可核对的差异，不是隐藏思维链
    repair_requirement: str


class RewriteReviewResult:
    verdict: str         # passed / needs_repair / blocked / inconclusive
    issues: list[ReviewIssue]
    checks: list[CheckOutcome]  # passed / failed / not_run / error
    policy_version: str
    usage: Usage


class StyleReviewPort:
    async def review(
        self, request: RewriteReviewRequest, grant: ReviewGrant
    ) -> RewriteReviewResult: ...
```

ReviewGrant 是调用方授予的只读调用额度、截止时间和追踪上下文，不赋予工具写权限。传入的 style_id、版本归属及 Hash 在可信边界校验，不能由用户消息指定任意产物。

结果一致性由程序验证：存在阻断问题不能返回 passed；必要检查没执行、超时或格式错误不能伪装成通过；判决不合法时按 inconclusive 处理。

### 10.2 三类检查

**完整性检查**：必要引用、claim／action／risk 标识、内容块覆盖、受保护字面量、输出结构、版本归属。精确文本保护、数值规范化和语义等价分别处理；不得为“简短”放宽现有受保护事实，也不把所有文字都机械要求逐字相同。

**语义保真检查**：对象、事实、结论、条件、否定、风险、不确定性、时间、数量、业务状态，以及无依据新增建议或关系假设。检查忠实原稿，不宣称重新验证了医学真伪。

**风格与表达检查**：按版本证据支持的边界，识别过度模仿、套话、生硬、无依据责备、过度压缩。明确的关键问题要求修复；偏好不确定时记录警告，不因为主观小分歧无限改写。

语义与表达结论逻辑上分离。实现可以在一个结构化审查模型调用中输出两个部分，完整性检查由代码执行；但生成与审查不能是同一个回复中自报“我已检查”。同模型分开调用仍有局限。

### 10.3 判决与修复职责

- passed：必要检查完成且无阻断问题；允许正常输出。
- needs_repair：指出可修复问题，由 Style Agent 执行有限修复。
- blocked：当前候选不能输出；Agent 使用原稿兜底或将源契约错误交还上游。
- inconclusive：无法完成可靠判断；不把不确定当通过，按剩余预算及兜底策略处理。

审查组件只提供问题和修复要求，不生成新回答、不调用其他 Agent、不修改素材或 Guide。训练器对许多结果归纳后修改下一候选；线上 Agent 只修复本轮输出。

### 10.4 审查独立版本化

ReviewPolicy 独立于 StylePackage，包含规则代码版本、模型及参数、审查 Prompt／Schema、严重程度规则和校准集版本。版本报告保存实际审查签名，不能只记一个可变模型别名。

审查升级使用独立的已标注校准用例，检查误报、漏报、调用异常和延迟；至少包含“合理缩短”“数字等价表达”“保存失败被改成功”“鼓励替代问题”“新增责备”等情况。

训练器没有修改 ReviewPolicy 的权限。审查策略升级后，使用同一新策略重跑对照与候选；现用风格也必须回归检查后再明确部署或切换审查配置。升级不覆盖旧报告，不能把标准变化算成风格提升。

## 11. 人工验收与启用

Guide 与示例一致，只证明内部自洽，不证明符合用户认可的医生风格。保留最终人评，不要求每个中间候选逐个审批。

最终用例先选定再生成结果，主要显示自然用户输入、必要语境、实际医生回答和可方便查看的改写前原文。当前启用版回答可展开比较，不恢复独立 A/B 管理入口。

人工暂沿用已有“风格匹配、语义忠实、表达适宜”三项评分；自然程度和整体一致性在自动报告中解释，不因本轮架构调整新增必填评分项。旧草案的评分替换建议不视为已经确认。

- 接受：说明可空，不自动加入正面素材。
- 拒绝：理由必填，期望回答可选；反馈在下一次明确构建时采用。
- 题目或自动判决有问题：标记复核，不直接修改自动原始结果来放行。
- 修订题目生成新题目修订和输出，旧评分不继承，补足有效题数。

推荐发布门槛：约定的最终用例全部完成人评并接受，无未解决关键语义错误、关键回归失败和影响验收的异常；产物与被评审内容一致。原稿兜底不能冒充成功风格改写来通过验收。

发布、启用单独执行。自动完成构建、人评接受、添加纠正都不切换线上版本。启用更新指针，仅影响新开始的风格调用，本轮重试继续使用锁定版本。

## 12. 后台信息架构与界面

```text
表达风格
└─ 风格列表 → 医生风格
   ├─ 构建版本
   ├─ 评审版本
   └─ 追加纠正素材
      ├─ 上半页：四字段手工表单
      └─ 下半页：素材库（分页）
         ├─ 未参与构建
         └─ 已参与构建
```

沿用既有后台左侧导航、颜色、卡片、面包屑。参考图：[风格列表](docs/ui-mockups/style-list.png)、[构建版本](docs/ui-mockups/style-build.png)、[评审版本](docs/ui-mockups/style-review.png)、[纠正素材](docs/ui-mockups/style-corrections.png)。图中的早期字段和分类不覆盖本文要求。

构建页显示：全库数量、未参与修订数量、已筛选数量及理由；冻结→素材审查→规律提炼→联合构建→自动优化→最终验收→待人评的阶段；当前轮次、实际用量、异常原因、取消／恢复操作；最终结论与“这版改了什么”。不把后台候选数变成人工待办数。

评审页显示：最终候选、固定用例进度、逐条结果及拒绝框；所有关键异常和退步应可见，不只展示挑选出的好回答。独立题与附加回归分别标识，技术详情默认折叠。

素材页显示：用户输入、实际回答、可空期望、纠正意见、修订、最新处理角色及结果、构建关联。支持关键词／质量结果筛选和两个 Tab 各自分页，不能只展示最终被选作示例的那部分。

不新增第四个主子 Tab，不恢复“确认／询问／鼓励”等沟通行为分组，不增加自动 Trace 纠正入口。

### 12.1 构建过程：总览、阶段结果、按需详情

在现有“构建版本”子 Tab 内展示，不另开训练监控页面。默认阅读顺序：本次任务总览 → 阶段时间线 → 最终结论；点击某个阶段才展开其输入、产物和依据。已经结束的构建也能重新打开查看。

顶部总览展示当前阶段、当前动作、已处理／总数（分母已知时）、运行轮次、累计耗时、最后活动时间和实际用量。基线版本、冻结素材数量也在此展示；构建期间新增的素材明确标注为下一次使用，不混入本次计数。

| 阶段 | 默认显示什么 | 展开后能核对什么 |
| --- | --- | --- |
| 冻结素材 | 本次总数、新增／修订数、对照版本 | 本次素材清单、修订和已纳入的纠正意见 |
| 素材审查 | 已分析数量，可用、重复、冲突和缺少上下文等结果 | 每条素材的处理理由、建议用途；为什么未选作正面示例 |
| 提炼与联合构建 | 形成哪些表达规律、选入哪些示例、发现哪些矛盾 | Guide 原文、规则对应证据和反例、示例入选／淘汰依据、Guide 与示例一致性检查 |
| 自动优化 | 当前轮次、发现的问题、这轮改了什么、保留哪个候选 | 调整前后 Guide／示例差异，同题结果变化、退步项、候选保留或淘汰依据 |
| 最终评测 | 有效题数、通过／未通过／异常数、与对照的比较 | 用户输入、中性原稿、实际改写、共享审查结果；发生修复时展示每次输出、失败原因和最终采用／兜底结果 |
| 构建完成 | 建议采用／无明确提升／不建议采用／需要人工确认 | 完整版本差异、纠正意见处理结果、未解决问题、预算消耗及进入人评的入口 |

各阶段摘要由真实记录和产物生成，不能用模型生成的一段“正在认真分析”替代执行状态。用途可能多选，质量结果与用途分开统计，不把重叠分类的数量相加冒充素材总数。

优化轮次与模型请求重试分开显示：重新请求失败的调用，不应伪装成又完成一轮有效优化。最终评测中的关键失败、兜底和异常不能被成功用例折叠掩盖。

### 12.2 等待、更新与失败必须可解释

- 页面运行中自动刷新阶段和结果，首版建议使用约 2～3 秒的轻量轮询；间隔是实现建议，可调整，不要求引入独立实时服务。
- 区分排队、运行、等待模型、重试等待、失败、取消和完成；运行中的等待是当前动作，不冒充任务已经完成。
- 等待模型时显示用途、请求开始时间、已等待时长及超时上限；模型尚未返回就明确显示“尚无结果”。worker 心跳与业务进展分别记录，心跳仍在不等于素材处理数增加。
- 能确定数量时显示实际计数，例如“素材审查 36／80”；优化轮次只显示当前轮和预算上限，不能当作必需跑满的进度。不按时间编造整体百分比或承诺结束时间。
- 同时显示“最后活动”和“最后产物更新”。心跳过期、网络断开或状态读取失败要提示状态待确认，不继续假装正常运行，也不擅自宣告后台任务失败。
- 出错时显示发生阶段、可理解的原因、已有结果是否保留、是否可重试或需修改素材／配置。取消、恢复和新建任务必须区分，不让用户刷新页面来碰运气。
- 刷新、离开再回来、worker 重启后，可从已持久化记录恢复显示；页面不是训练任务的执行者，关掉页面不终止训练。

### 12.3 可核对的决策记录，不展示隐藏思维链

记录的是实际动作、可核对的依据和结果，例如“规则过度泛化，与素材 E17 矛盾，因此收窄适用范围”，并链接对应证据和真实差异。不能声称展示模型全部内部思考，也不能事后虚构决策过程。

训练器通过公开事件端口报告阶段、动作和结果，审查器返回结构化判决，不自行操作管理台数据库。建议记录 run_id、事件序号、阶段、优化轮次／候选、执行尝试、事件时间、状态、计数、简短说明、产物引用、用量和可恢复错误码。

阶段完成、计数与产物引用应一致持久化；不存在的产物不展示为已完成。重连按事件序号补取，前端去重，重试不能重复累加已处理素材数。大量逐条结果分页读取，不把全部文本塞进每次进度刷新。

管理员默认看业务摘要，技术详情按需展开模型／模板／审查版本、调用耗时、Token 和错误标识；不默认展示长 JSON、完整 Prompt 或密钥。可读素材与测试输出仍受管理员权限和风格隔离限制，最终验收集详情不能通过这些记录反向进入本轮候选优化器。

## 13. 代码组织与数据落点

### 13.1 功能边界

建议目标目录如下，具体小文件按实际规模合并，避免全局超大 contracts 或 utils：

```text
src/slim_guard/
├─ expression_style/             # 独立风格能力，无管理员 ORM 依赖
│  ├─ contracts.py               # 小型公开产物与改写契约
│  ├─ compilation.py             # 确定性编译固定风格 Prompt
│  ├─ trainer/
│  │  ├─ service.py              # 构建／优化流程
│  │  ├─ materials.py            # 素材审查、修订、证据
│  │  ├─ construction.py         # Guide 与示例联合构建
│  │  ├─ evaluation.py           # 数据集隔离、比较与报告
│  │  └─ ports.py                # 素材快照、产物和任务持久化端口
│  └─ review/
│     ├─ contracts.py
│     ├─ service.py              # 唯一单条回复审查入口
│     ├─ integrity.py
│     └─ policy.py               # 语义／表达 Prompt 与版本
├─ agents/style/                 # 唯一在线／离线改写执行路径
│  ├─ agent.py
│  └─ renderer.py
└─ style_management/             # 管理 API、ORM、Repository、任务适配
   ├─ worker.py                  # 薄租约／调度入口
   └─ runtime.py                 # 通过公开端口提供已发布快照
```

训练器通过改写端口调用 Style Agent，Style Agent 只依赖风格公共契约和 review 的公开端口。review 不反向导入 trainer、管理后台或具体 Repository。依赖由应用装配层注入，不靠运行时动态导入互相调用的私有模块。

前端继续使用 frontend/src/components/style/。结构迁移与行为变更分阶段提交，最终删除旧内嵌审查、风格专用动态检索和逐素材人评路径，不长期维护两套流程。

### 13.2 最小持久化模型

保留现有四张新制风格表，按职责调整，并新增一个构建任务记录以容纳多个自动候选：

| 对象 | 必要变化 |
| --- | --- |
| expression_styles | 继续保存风格和 active_version_id，不恢复环境变量启用入口 |
| expression_examples | 增加 correction_opinion、revision；desired_response 可空但与意见至少一项非空；不把全局 status 当某版筛选结论 |
| expression_build_runs（新增） | 全库快照、基线、反馈消费、阶段及可恢复事件记录、心跳／最后产物时间、预算、租约、各素材处理结果、候选及评测引用、最终报告 |
| expression_versions | 冻结 StylePackage、编译 Prompt、内容 Hash、构建关联；人评／发布状态与构建任务分离 |
| expression_review_cases | 改关联 test_case_id／revision／题族，保留可选素材来源；保存实际输出、审查和执行签名、人评 |

初期中间候选、素材处理结果和测试清单可作为有 schema_version 的结构化快照存于构建记录，不为每个名词新增一张表。最终人评记录仍独立保存便于分页。

构建完成写入产物／报告及素材修订关联应原子化。修订正文完整冻结，后续编辑或删除不破坏已有版本；不会为了追踪而恢复旧系统的独立历史审计平台。

### 13.3 状态与 API

构建任务：queued → running → completed；失败／取消分别 failed／cancelled。completed 的报告仍可能是无提升或不推荐，不等于质量通过。

最终版本：ready_for_review → published；拒绝保留其产物与反馈，不原地改写。是否 active 由启用指针表示。自动中间候选只属于构建记录，不全部进入人工版本列表。

继续以 /api/admin/styles 为管理入口，增加素材修订及 participation 筛选、构建任务详情／取消／恢复、最终报告。管理员认证、CSRF、风格隔离、同风格构建互斥、幂等键继续执行。

任务详情提供轻量状态摘要，阶段事件支持按序号增量读取，素材结果、候选差异和测试详情分别分页／按需读取。事件的具体存储可随规模选择，但不能只存在 worker 内存或浏览器状态中。

线上仅可解析已发布并明确启用的版本。离线评测通过内部受限端口显式传入候选包，不能为了评测放宽线上版本读取权限。候选包与所属 style_id 不符时拒绝。

## 14. 迁移、验证与验收

### 14.1 实施顺序

1. 提取共享审查接口，保持现有行为并验证预算、修复与兜底不变。
2. 引入版本化审查策略及表达检查，增加校准／误报漏报回归。
3. 实现 StylePackage 编译、固定示例执行；替换风格专用动态检索路径。
4. 实现训练器、全库筛选、联合构建、自动优化和分离评测。
5. 迁移素材字段／修订、任务与人评契约，完成三个子 Tab 及素材两 Tab。
6. 定向及跨模块回归、隔离模型验收；实际提交推送和部署另按用户授权执行。

现有素材不因本设计清空；不假定当前素材只有 8 条。旧已启用版本的动态示例库不能简单改名成“精选固定包”：需要重新构建验收，或经明确批准使用中性兜底完成切换。旧版本状态、人评不伪装为新流程已通过。

实施前核对真实数据范围和启用状态，准备必要备份及切换顺序；废弃的风格字段、任务路径和表关系在替换后清理，不动身份、健康记录、记忆、营养 RAG、COS。本文不授权立即做任何数据清理。

### 14.2 核心验收用例

- 纯人工四字段录入；期望或意见至少一项，只有意见也能保存，不能伪造人工期望。
- 两个素材 Tab 覆盖全部现存素材；失败任务不提前改变参与状态；修改旧素材重新待构建。
- 下一次快照包含已参与和未参与素材，以及上次自动排除项；判决关联具体修订和构建。
- 噪声素材不因数量多主导 Guide；负面示例不被当作正面回答模仿。
- Guide／示例共同证据可追溯；反例能收窄规则，冲突不被静默掩盖。
- 无固定 8 组要求；构建预算有限，产物过大不线上截断；输出无改善结论不伪称提升。
- 线上和离线使用同一改写及审查路径，不调用训练器或风格检索；候选读取权限不泄漏。
- 保留数字、身份、状态、条件；合理缩短不因字面减少自动判错；未解决语义问题不被风格分抵消。
- 所有必要检查有明确状态；审查超时、格式错误、额度不足不放行未经检查输出。
- 修复带具体原因，最多一次；兜底有真实原因且原稿已获上游放行。
- 开发／最终验收题族隔离；最终结果不回灌当前构建，反馈消费后重新标识并补题。
- 最终人评数量不随素材数或自动候选数增长，接受说明可空、拒绝理由必填。
- 冻结产物和已评输出不能被重试覆盖；保存／构建／接受均不自动启用。
- 审查升级可独立测试但不能由训练器放松标准；跨审查版本的分数不伪装成风格提升。
- 不恢复旧 A/B 页面或场景分类；营养 RAG 的向量、检索和发布回归不受影响。
- 构建页能查看每阶段输入、结果和依据；可追踪具体素材为什么排除、规则由哪些证据产生、每轮为何保留或放弃候选。
- 等待模型、重试等待、心跳过期、读取失败有不同提示；没有伪造进度、耗时或隐藏思维链。
- 刷新、断线重连、worker 恢复后过程可重现，事件不重复、计数不虚增；大量素材和测试结果仍可分页查看。
- 审查失败到有限修复再到采用／兜底的每步均有实际输出和原因；最终报告不隐藏退步或异常，技术日志默认折叠。

按 [AGENTS.md](AGENTS.md) 选择受影响测试，重点覆盖素材契约／状态、训练器、共享审查、Style Agent、预算与权限、数据库迁移及前端分页／评审。跨模块和迁移改动扩大回归范围；真实模型验收与业务单测分开报告，不默认每次开发全量运行。

## 15. 研究依据与结论边界

- [Catch Me If You Can? Not Yet，EMNLP 2025](https://aclanthology.org/2025.findings-emnlp.532/)：在其英文作者风格任务中，示例有帮助，但增加数量收益有限，按内容相似性选例可能降低作者辨识表现。它不证明“固定 8 组最佳”，也不证明所有动态检索都无效。本文选择固定包是结合本项目保真、可复现和实现复杂度作出的工程决策。
- [Whose story is it?，IJCNLP-AACL 2025](https://aclanthology.org/2025.ijcnlp-long.82/)：使用主张与原文证据构建作者表达画像，为 Guide 的证据化提炼提供参考；故事生成结论不直接等同中文医生短对话效果。
- [ProTeGi，EMNLP 2023](https://aclanthology.org/2023.emnlp-main.494/)：根据错误反馈提出提示修改，再用数据选择候选，为有界自动优化提供方法参考。本文不照搬其任务指标，也不保证每次自动修改都有收益。
- [Panza](https://arxiv.org/abs/2407.10994)：说明微调可以在个人邮件任务中取得有效结果。本阶段不采用微调是范围和工程选择，不声称提示方案在所有任务中优于微调。

本设计尚未完成项目内模型实测。自动检查、版本通过率和少量人评不能证明完整克隆真人；不以“医生风格”冒充医生本人或其专业授权。
