# 表达风格管理 UI 设计

本页按最新讨论同步目标交互，技术方案以 [风格训练器、Style Agent 与共享审查组件设计](STYLE_TRAINER_SHARED_REVIEW_DESIGN.md) 为准，不能视为已经上线。[独立评测旧草案](STYLE_EVALUATION_ITERATION_DESIGN.md) 仅作历史参考，不作为本轮需求或验收依据。四张效果图继续作为布局参考，旧字段和分类不覆盖最新文字要求。

## 1. 目标

为后台管理员提供一个清晰的表达风格管理入口。当前默认风格是“医生风格”，未来可以添加其他风格。页面必须让管理员能够：

- 查看和选择风格；
- 构建新的风格版本；
- 逐条评审模型输出；
- 追加纠正素材，并查看全部示例；
- 理解当前版本、发布状态和示例是否已经进入下一次构建。

本设计采用“风格列表 → 风格详情 → 详情子 Tab”的两级结构，避免把构建、评审和素材管理散落在后台主导航中。

## 2. 页面层级

```text
后台主导航
└── 表达风格
    ├── 风格列表
    └── 医生风格（风格详情）
        ├── 构建版本
        ├── 评审版本
        └── 追加纠正素材
            └── 页面下半部分：完整素材库（分页）
                ├── 未参与构建
                └── 已参与构建
```

左侧主导航只保留一个“表达风格”入口。三个功能不是主导航项，也不是风格列表页的 Tab；只有点击具体风格后，才显示三个子 Tab。

## 3. 全局视觉和布局规范

- 桌面端优先，基准画布约 1440×900，内容区支持滚动。
- 左侧固定深绿色导航栏，宽度约 240–270px。
- 内容区使用暖白/浅灰背景，卡片使用白色、圆角和浅边框。
- 主色为深绿，成功状态使用绿色，待处理使用琥珀色，拒绝/错误使用红色。
- 页面标题使用大号深色字体；说明文字使用灰绿色，减少技术术语。
- 卡片之间保留明显间距，避免把流程日志、版本信息和操作按钮混成一块。
- 所有按钮使用明确动词，例如“构建新版本”“接受”“拒绝”“加入示例库”。

左侧导航固定包含：

1. 用户中心
2. 表达风格（当前选中）
3. 营养知识库

旧的 A/B 人评、风格版本和风格纠正页面以及兼容路由全部删除；新的操作统一进入“表达风格”及其风格详情子 Tab。

## 4. 风格列表页

参考图：[表达风格列表页](docs/ui-mockups/style-list.png)

### 页面结构

1. 页面标题：`表达风格`
2. 副标题：说明这里管理多个可复用的回复风格。
3. 概览卡片：
   - 风格总数
   - 当前启用风格
   - 示例总数
4. 风格列表：每个风格显示为一张卡片。

### 风格卡片字段

- 风格名称，例如“医生风格”；
- 状态徽章：系统默认、已启用、未启用；
- 简短描述，例如“专业、严谨、友善”；
- 示例数量；
- 创建时间或最后更新时间；
- 点击卡片进入该风格详情。

当前只展示“医生风格”。页面保留“新建风格”卡片，为未来新增其他风格预留入口。

## 5. 风格详情公共区域

参考图：[医生风格详情页](docs/ui-mockups/style-build.png)

点击风格卡片后，页面顶部显示：

- 面包屑：`表达风格 / 医生风格`；
- 风格名称；
- `系统默认`、`已启用`等状态；
- 风格简介；
- 创建时间、最后更新、适用场景。

公共区域下面显示三个子 Tab：

- 构建版本
- 评审版本
- 追加纠正素材

切换子 Tab 时保留风格上下文，不返回风格列表。

## 6. 构建版本

参考图：[构建版本](docs/ui-mockups/style-build.png)

### 页面内容

- 当前版本：例如 `doctor_strict_v3`；
- 当前版本状态：已评审、已发布、已启用等；
- Style Guide 摘要；
- 全库素材数量及尚未参与构建的修订数量；
- 主操作按钮：`构建新版本`；
- 自动比较结论与“这版改了什么”；
- 历史版本列表。

Style Guide 摘要展示实际生成的表达规律、适用边界、固定示例及证据关联，不将“专业、友善、先结论后解释”等描述硬编码成所有版本的医生风格。

不改变原意、事实和业务状态等共同约束另行展示，不冒充从医生素材中学到的个人特征。

### 构建流程可视化

点击“构建新版本”后，以横向步骤条显示：

1. 冻结全库素材与反馈
2. 素材审查、规律与证据整理
3. 联合构建 Guide 与固定示例
4. 有界自动优化和开发评测
5. 最终独立验收及版本比较
6. 待人工评审

步骤条展示当前步骤、完成步骤、优化轮次、实际用量、失败步骤和失败原因。无明确提升也要如实显示；自动中间候选不增加人工待办。构建完成后不会自动替换线上版本，必须经过评审、发布和启用。

### 训练过程必须可见

不能只有步骤条或滚动日志。在“构建版本”内按“总览 → 阶段时间线 → 最终结论”展示，点击阶段展开结果，不新增第四个子 Tab。

- 总览：当前动作、实际处理数、运行轮次、耗时、最后活动和用量；能看清是在处理素材、等待模型还是准备重试。
- 素材审查：哪些可用、哪些排除或冲突，以及逐条理由；全部结果支持分页。
- Guide 与示例：具体生成内容、证据来源、入选原因及冲突处理。
- 自动优化：每轮修改前后差异、效果变化、为什么保留或放弃候选。
- 评测：原稿、改写、审查问题、带原因的重试输出、最终采用或兜底；关键异常和退步不能隐藏。
- 完成后：与启用版相比是否改进、纠正意见如何处理、剩余问题，提供进入人评的入口。

运行中自动更新，刷新或重进页面不丢过程。等待时显示用途、时长和超时上限；读取失败与后台任务失败分开。不编造整体百分比，不把日志或模型隐藏思维链作为主界面。完整契约见 [主设计第 12 节](STYLE_TRAINER_SHARED_REVIEW_DESIGN.md#12-后台信息架构与界面)。

## 7. 评审版本

参考图：[评审版本](docs/ui-mockups/style-review.png)

评审页保持简单，不展示过多技术字段。顶部显示：

- 待评审数量；
- 已接受数量；
- 已拒绝数量；
- 当前评审进度。

### 单条评审卡片

依次展示：

1. 用户输入
2. 必要的简短语境，以及方便查看的改写前原文
3. 最终候选版本实际生成的医生回答
4. 期望医生回答（人工可选填写，不伪装成已有标准答案）

评审对象为约 10～15 条独立测试的实际输出，不是逐条评审原素材，也不是所有自动优化候选。当前启用版的同题回答可展开比较，不新增 A/B 管理入口。

然后提供三个评分项：

- 风格匹配
- 语义忠实
- 表达适宜

评分采用 1–5 分选择，不要求接受时填写额外说明。

### 操作规则

- 点击“接受”：保存评分，评审说明可选；
- 点击“拒绝”：必须填写拒绝理由后才能保存；
- 拒绝理由及期望回答用于下一次明确构建，不直接修改当前版本；
- 接受不自动将模型回答加入正面素材库；
- 开发期不保留独立历史审计：未发布版本可以更正评分；已发布版本只读，不覆盖产物。

右侧可显示当前 Style Guide 摘要和评审进度，但不应遮挡主要评审内容。

## 8. 追加纠正素材

参考图：[追加纠正素材](docs/ui-mockups/style-corrections.png)

该页面同时承担“新增素材”和“查看全部示例”两个职责，不单独增加“示例库”子 Tab。

### 上半部分：新增纠正素材

表单字段：

- 用户输入（必填）；
- 医生回答（必填，已经实际发生的回答）；
- 期望医生回答；
- 纠正意见，可补充希望保留或避免的表达。

建议期望回答与纠正意见至少填写一项，以支持纯负面纠正；这是新设计的字段建议。当前纯人工手写，不自动带入聊天记录或 Trace。

提交按钮：`加入示例库`。

系统自动记录：

- 添加人；
- 添加时间；
- 来源（后台手工填写；历史素材保留原来源，不新增自动 Trace 导入）；
- 所属风格；
- 当前素材修订的参与状态：未参与构建。

新增素材不会立即修改线上已启用版本，也不需要指定某个版本。下一次点击构建会冻结已参与和未参与两个 Tab 的全部现存素材，统一分析筛选，不只使用新增内容。负面、冲突或内容纠正不因人工提交就自动成为正面示例。

### 下半部分：全部示例

在页面内部设“未参与构建”“已参与构建”两个 Tab，各自支持分页。二者合起来覆盖完整素材库，包括未被选作固定示例的素材。每行至少展示：

- 用户输入；
- 医生回答；
- 期望医生回答；
- 纠正意见；
- 素材用途与质量结果（正面表达、负面意见、内容纠正、语义漂移、重复、冲突、缺少上下文等）；
- 来源；
- 添加时间；
- 当前修订、最新处理理由及关联构建；
- 操作菜单。

支持的筛选和操作：

- 按素材质量分类筛选，不再显示确认、询问、解释、鼓励等场景分组；
- 按实际素材来源筛选，不自动制造“评审接受素材”；
- 按处理结果筛选：用于规则、用于示例、负面约束、被排除、待解决冲突；
- 关键词搜索；
- 分页；
- 查看完整内容和进入下一次构建的版本记录。

页面应明确提示：参与构建不等于被采用或已上线。当前修订只有在构建完整产出分析与报告后才算已参与；失败、取消或仍运行的任务不提前改变状态。修改素材生成新修订，回到未参与构建；已生成版本的冻结副本不变。

示例库是全量素材池；构建时会冻结快照，生成新的 Style Guide、固定示例和不可变版本。上次自动排除或待确认项仍参与下一次全库分析。

## 9. 状态和权限提示

- 已启用版本：当前运行时使用的版本，只读展示；
- 待评审版本：只能评审，不能全量启用；
- 已拒绝版本：保留用于回溯，不参与运行时；
- 未参与构建：当前修订尚未被完整结束的构建处理；
- 已参与构建：当前修订已被处理，可能采用、排除或待确认，不代表已发布；
- 构建中：禁止重复启动同一风格的构建任务；
- 高风险操作（发布、全量启用）必须有明确确认。

## 10. 实现还原要求

前端实现应优先复用现有后台的颜色、字体、卡片、状态徽章、按钮和侧边栏组件。页面不能把技术 trace、Agent 调度日志或原始模型字段直接作为主界面内容；这些内容只在需要时通过“查看详情”展开。

四张效果图是布局和层级参考，不要求逐像素复制，但必须保持：

- 左侧导航位置和选中态；
- 风格列表与风格详情的两级关系；
- 详情页三个子 Tab；
- 构建、评审、追加素材的主操作顺序；
- 追加素材页下方的分页示例列表。

图中旧版本号和原先场景标签仅为早期占位内容，不作为实现要求。目标数据和迁移方案见 [风格训练器设计](STYLE_TRAINER_SHARED_REVIEW_DESIGN.md)，当前已实现行为见 [操作手册](STYLE_MANAGEMENT.md)，不混淆二者。实际页面显示 API 返回的计数、版本和素材，不使用硬编码示例数，不恢复旧 A/B 页面。
