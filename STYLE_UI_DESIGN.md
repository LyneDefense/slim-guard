# 表达风格管理 UI 设计

## 1. 目标

为后台管理员提供一个清晰的表达风格管理入口。当前默认风格是“医生风格”，未来可以添加其他风格。页面必须让管理员能够：

- 查看和选择风格；
- 构建新的风格版本；
- 逐条评审模型输出；
- 追加纠正素材，并查看全部示例；
- 理解当前版本、发布状态和示例是否已经进入下一次构建。

本设计采用“风格列表 → 风格详情 → 详情子 Tab”的两级结构，避免把构建、评审和素材管理散落在后台主导航中。

## 2. 页面层级

```text
后台主导航
└── 表达风格
    ├── 风格列表
    └── 医生风格（风格详情）
        ├── 构建版本
        ├── 评审版本
        └── 追加纠正素材
            └── 页面下半部分：全部示例（分页）
```

左侧主导航只保留一个“表达风格”入口。三个功能不是主导航项，也不是风格列表页的 Tab；只有点击具体风格后，才显示三个子 Tab。

## 3. 全局视觉和布局规范

- 桌面端优先，基准画布约 1440×900，内容区支持滚动。
- 左侧固定深绿色导航栏，宽度约 240–270px。
- 内容区使用暖白/浅灰背景，卡片使用白色、圆角和浅边框。
- 主色为深绿，成功状态使用绿色，待处理使用琥珀色，拒绝/错误使用红色。
- 页面标题使用大号深色字体；说明文字使用灰绿色，减少技术术语。
- 卡片之间保留明显间距，避免把流程日志、版本信息和操作按钮混成一块。
- 所有按钮使用明确动词，例如“构建新版本”“接受”“拒绝”“加入示例库”。

左侧导航固定包含：

1. 用户中心
2. 表达风格（当前选中）
3. 营养知识库

旧的 A/B 人评、风格版本和风格纠正页面只保留兼容路由，不再出现在主导航；新的操作统一进入“表达风格”及其风格详情子 Tab。

## 4. 风格列表页

参考图：[表达风格列表页](docs/ui-mockups/style-list.png)

### 页面结构

1. 页面标题：`表达风格`
2. 副标题：说明这里管理多个可复用的回复风格。
3. 概览卡片：
   - 风格总数
   - 当前启用风格
   - 示例总数
4. 风格列表：每个风格显示为一张卡片。

### 风格卡片字段

- 风格名称，例如“医生风格”；
- 状态徽章：系统默认、已启用、未启用；
- 简短描述，例如“专业、严谨、友善”；
- 示例数量；
- 创建时间或最后更新时间；
- 点击卡片进入该风格详情。

当前只展示“医生风格”。页面保留“新建风格”卡片，为未来新增其他风格预留入口。

## 5. 风格详情公共区域

参考图：[医生风格详情页](docs/ui-mockups/style-build.png)

点击风格卡片后，页面顶部显示：

- 面包屑：`表达风格 / 医生风格`；
- 风格名称；
- `系统默认`、`已启用`等状态；
- 风格简介；
- 创建时间、最后更新、适用场景。

公共区域下面显示三个子 Tab：

- 构建版本
- 评审版本
- 追加纠正素材

切换子 Tab 时保留风格上下文，不返回风格列表。

## 6. 构建版本

参考图：[构建版本](docs/ui-mockups/style-build.png)

### 页面内容

- 当前版本：例如 `doctor_strict_v3`；
- 当前版本状态：已评审、已发布、已启用等；
- Style Guide 摘要；
- 全局示例库数量；
- 主操作按钮：`构建新版本`；
- 历史版本列表。

Style Guide 摘要至少展示：

- 角色定位：专业、友善、可信；
- 语言风格：严谨、直接、易理解；
- 表达结构：先结论，后解释；
- 禁止事项：不改变原意、不做无依据诊断、不夸大效果。

### 构建流程可视化

点击“构建新版本”后，以横向步骤条显示：

1. 冻结示例库
2. 生成 Style Guide
3. 整理和分类样例
4. 自动评测
5. 待人工评审

步骤条展示当前步骤、完成步骤、失败步骤和失败原因。构建完成后不会自动替换线上版本，必须经过评审、发布和启用。

## 7. 评审版本

参考图：[评审版本](docs/ui-mockups/style-review.png)

评审页保持简单，不展示过多技术字段。顶部显示：

- 待评审数量；
- 已接受数量；
- 已拒绝数量；
- 当前评审进度。

### 单条评审卡片

依次展示：

1. 用户输入
2. 当前医生回答
3. 期望医生回答

然后提供三个评分项：

- 风格匹配
- 语义忠实
- 表达适宜

评分采用 1–5 分选择，不要求接受时填写额外说明。

### 操作规则

- 点击“接受”：保存评分，评审说明可选；
- 点击“拒绝”：必须填写拒绝理由后才能保存；
- 拒绝理由用于后续 Style Guide 和样例修正；
- 评审结果以追加记录方式保存，不覆盖历史评审。

右侧可显示当前 Style Guide 摘要和评审进度，但不应遮挡主要评审内容。

## 8. 追加纠正素材

参考图：[追加纠正素材](docs/ui-mockups/style-corrections.png)

该页面同时承担“新增素材”和“查看全部示例”两个职责，不单独增加“示例库”子 Tab。

### 上半部分：新增纠正素材

表单字段：

- 用户输入（必填）；
- 医生回答（必填，已经实际发生的回答）；
- 期望医生回答（必填）。

提交按钮：`加入示例库`。

系统自动记录：

- 添加人；
- 添加时间；
- 来源（后台手动添加、Trace 纠正等）；
- 所属风格；
- 当前状态：待构建。

新增素材不会立即修改线上已启用版本，也不需要指定某个版本。它会进入该风格的完整示例库，并在下一次构建时统一使用。

### 下半部分：全部示例

展示当前风格的完整示例库，支持分页。每行至少展示：

- 用户输入；
- 医生回答；
- 期望医生回答；
- 示例分类/场景；
- 来源；
- 添加时间；
- 状态；
- 操作菜单。

支持的筛选和操作：

- 按场景分类筛选；
- 按来源筛选：历史导入、人工评审、纠正素材；
- 按状态筛选：已采用、待构建、已拒绝；
- 关键词搜索；
- 分页；
- 查看完整内容和进入下一次构建的版本记录。

页面应明确提示：示例库是全量素材池；构建时会冻结一个快照，生成新的 Style Guide、样例集合和不可变风格版本。

## 9. 状态和权限提示

- 已启用版本：当前运行时使用的版本，只读展示；
- 待评审版本：只能评审，不能全量启用；
- 已拒绝版本：保留用于回溯，不参与运行时；
- 待构建示例：已进入全局示例库，但尚未进入任何新版本；
- 构建中：禁止重复启动同一风格的构建任务；
- 高风险操作（发布、全量启用）必须有明确确认。

## 10. 实现还原要求

前端实现应优先复用现有后台的颜色、字体、卡片、状态徽章、按钮和侧边栏组件。页面不能把技术 trace、Agent 调度日志或原始模型字段直接作为主界面内容；这些内容只在需要时通过“查看详情”展开。

四张效果图是布局和层级参考，不要求逐像素复制，但必须保持：

- 左侧导航位置和选中态；
- 风格列表与风格详情的两级关系；
- 详情页三个子 Tab；
- 构建、评审、追加素材的主操作顺序；
- 追加素材页下方的分页示例列表。
