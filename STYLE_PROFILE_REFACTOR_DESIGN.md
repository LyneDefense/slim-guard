# 表达风格系统重构设计

## 1. 文档目的

本文定义 SlimGuard 表达风格系统的重构方向。目标是把风格系统从“按场景分类后套用示例”改为“基于 Style Guide 和相似表达样例的纯表达转换”，同时支持多套可选风格、持续追加纠正素材和可审计的版本发布。

本文只讨论表达风格，不负责用户意图识别、业务工具选择、营养判断或医疗安全判断。

## 2. 核心原则

1. 风格转换只改变表达方式，不改变 Core 已确定的语义、事实、数字、单位、时间、风险、不确定性、引用和记录状态。
2. Style Agent 不负责判断用户意图，不负责业务场景分类，不负责重新选择专业 Agent。
3. Style Guide 是风格版本的主要约束，样例是增强参考，不是必需依赖。
4. 找不到相似样例时，仍然可以仅依据 Style Guide 完成转换。
5. 风格版本是不可变快照。新增纠正素材不会直接修改线上版本。
6. 线上运行时只使用已发布并启用的一个风格版本。
7. 构建版本不是基础模型微调，而是重新生成 Style Guide、整理样例并形成新的 Style Profile 快照。
8. 所有素材、构建、评审、发布和启用动作都必须可追踪、可回滚。

## 3. 用户可见的信息架构

后台新增或重构为一个“表达风格”入口。页面不把“构建版本”“评审版本”“追加纠正素材”拆成三个导航 Tab，而是在一个风格详情页内组织为三个部分。

### 3.1 风格列表

系统支持多套独立风格，例如：

- 医生风格；
- 温和陪伴风格；
- 简洁提醒风格。

每套风格显示：

- 风格名称；
- 描述；
- 当前 active 版本；
- 最近一次构建状态；
- 是否为系统默认风格；
- 是否允许用户选择。

当前系统默认风格为医生风格。未来用户可以选择其他已经发布的风格；如果用户没有个人选择，则使用系统默认风格。

### 3.2 风格详情页

进入某套风格后，在同一页面展示：

1. 构建版本；
2. 评审版本；
3. 追加纠正素材。

页面不展示运行时内部的 Coordinator、ResponsePlan、Baseline、Candidate 或复杂图节点作为主要操作对象。必要的技术详情可以折叠展示。

## 4. 风格数据模型

### 4.1 风格（Style Profile）

风格是长期存在的逻辑对象，例如“医生风格”。它不是某一个具体版本。

建议字段：

- `style_id`；
- `stable_name`；
- `display_name`；
- `description`；
- `is_system_default`；
- `is_user_selectable`；
- `status`；
- `active_version_id`；
- 创建和更新时间。

### 4.2 风格版本（Style Profile Version）

版本是某次构建得到的不可变快照，包含：

```text
Style Profile Version
├─ Style Guide
├─ Style Examples 快照
├─ 构建来源快照
├─ 自动评测结果
├─ 人工评审结果
├─ 发布凭证
└─ 启用记录
```

建议字段：

- `version`；
- `style_id`；
- `status`：draft、evaluated、ready_for_review、published、active、retired；
- `style_guide`；
- `examples_snapshot_sha256`；
- `source_corpus_snapshot_sha256`；
- `build_run_id`；
- `created_at`；
- `published_at`；
- `activated_at`。

版本发布后不能原地修改。任何规则或样例变化都必须构建新版本。

### 4.3 Style Guide

“Style Guide”比“语气指引”更准确。它描述表达方式，而不是业务判断。

建议包含：

- 总体语气：自然、克制、直接、专业但不冒充医生；
- 句子长度和段落数量；
- 直接程度和解释深度；
- 常用句式和收尾方式；
- 如何表达确认、追问、不确定性和限制；
- 是否使用表情、称呼和口语化表达；
- 禁止表达和高风险措辞；
- 不应主动添加的内容；
- 从样例中总结出的稳定规则；
- 每条规则的证据数量、置信度和来源样例。

Style Guide 不应包含具体用户事实、单条对话的数字、菜名、体重或一次性建议。

### 4.4 全局示例库

示例库是跨版本持续积累的全局素材库，不绑定到某一个版本。构建版本时读取的是示例库在构建时的快照。

示例可以来自：

- 人工追加纠正素材；
- 评审接受的样例；
- 已有历史风格样例迁移；
- 经人工确认的高质量线上案例。

示例建议包含：

- `example_id`；
- `user_input`；
- `neutral_response` 或历史医生回答；
- `desired_response`；
- `source_trace_id`；
- `source_style_version`；
- 创建人和创建时间；
- 质量评分；
- 提取出的表达差异；
- `corpus_status`：pending、approved、excluded、retired、conflict；
- 是否已经参与某些构建版本。

示例库是追加优先、可审计的。错误或过时素材不物理删除，而是标记为 excluded 或 retired。

## 5. 追加纠正素材

追加纠正素材只需要填写：

```text
用户输入
医生回答
期望医生回答
```

含义如下：

- 用户输入：用户当时实际说了什么；
- 医生回答：系统已经实际生成的回复；
- 期望医生回答：测试人员认为更合适的表达。

不要求填写：

- 场景分类；
- communication act；
- 目标版本；
- 专业 Agent；
- Prompt；
- Style Guide 规则。

系统自动记录来源 Trace、当时使用的风格版本、创建人和时间，但这些不是用户需要手填的字段。

保存后素材进入全局示例库，不会立即修改线上风格，也不会自动改变当前 active 版本。

系统应在后台展示：

- 是否已纳入某次构建；
- 是否被提取为风格规则；
- 是否与其他样例冲突；
- 是否被排除以及原因。

## 6. 构建新版本

用户在风格详情页点击“构建新版本”。构建过程如下：

```text
选择基础版本
  ↓
冻结全局示例库快照
  ↓
提取每条样例的表达差异
  ↓
剥离具体事实和一次性业务内容
  ↓
汇总重复出现的表达规律
  ↓
检测相互冲突的规则
  ↓
生成新的 Style Guide
  ↓
整理新的 Style Examples
  ↓
生成冻结评测集
  ↓
运行自动评测
  ↓
生成 draft 版本
```

这里的“训练”在产品界面中统一称为“构建版本”。当前方案不修改基础模型参数，也不执行模型微调。

### 6.1 表达差异提取

模型需要区分：

- 具体内容差异；
- 语气差异；
- 句式差异；
- 信息组织差异；
- 禁止表达；
- 是否新增了业务建议。

例如期望回复中出现“搭配蔬菜”，不能直接总结为“医生风格总是主动提供饮食建议”，除非多个独立样例和人工评审都支持这一规则。

### 6.2 Style Guide 演化

规则按稳定性分层：

- 稳定规则：多个高质量样例重复支持；
- 候选规则：只有少量样例支持；
- 冲突规则：不同样例给出相反方向。

只有稳定规则可以自动进入 Style Guide 草案。候选规则和冲突规则需要在构建结果中明确标记，由人工确认。

## 7. 版本评审

评审页面保持简单，不展示复杂的 Baseline/Candidate 内部术语。

每条评审只展示：

```text
用户输入
当前医生输出
期望医生输出
```

人工填写三个评分：

- 风格匹配：是否符合该风格的表达方式；
- 语义忠实：是否准确保留原意；
- 表达适宜：是否自然、清楚、克制、不冒犯。

操作按钮：

- 接受：评分说明可选；
- 拒绝：弹出拒绝理由框，理由必填。

拒绝后：

- 记录评分和拒绝理由；
- 不把该结果作为已批准样例；
- 拒绝理由作为下一次 Style Guide 构建的输入；
- 不修改当前 active 版本。

版本页面显示整体进度：

```text
待评审：8
已接受：5
已拒绝：2
未处理：1
```

自动评测是辅助信息，不替代人工评审。自动评测重点检查语义保真、禁用表达、事实和风险边界。

## 8. 发布和启用

版本状态分离：

```text
draft
  ↓
evaluated
  ↓
ready_for_review
  ↓
published
  ↓
active
```

发布表示版本具备使用资格；启用表示它成为该风格的当前线上版本。

启用新版本时保存：

- 风格 ID；
- 旧版本；
- 新版本；
- 操作人；
- 操作时间；
- 高风险确认文本；
- 发布和评审摘要。

回滚只需要重新启用上一个已发布版本，不修改任何历史版本。

## 9. 运行时应用

运行时的选择顺序：

```text
用户个人选择的风格
  ↓ 没有个人选择
系统默认风格（当前为医生风格）
  ↓
读取该风格的 active 版本
```

Style Agent 的输入：

- Core 已生成的中性回复；
- active 版本的 Style Guide；
- 通过检索找到的 0～3 条相似表达样例；
- 语义和安全保护约束。

不再使用：

- 按 `communication_act` 的样例分组路由；
- 根据工具调用猜测表达场景；
- 默认把未知回复归为鼓励；
- 多个风格版本同时竞争生成；
- Legacy、Shadow 或 Candidate 回复链路。

### 9.1 样例检索

样例采用混合检索：

- 关键词或短语匹配；
- 向量相似度；
- 当前风格和审核状态过滤；
- 相似度与样例质量重排。

最终取少量高质量样例作为 one-shot 或 few-shot 参考。

### 9.2 找不到相似样例

没有可靠相似样例时：

```text
Style Guide + 中性回复 → Style Agent
```

不能因为缺少样例而套用无关样例。Style Guide 是必需的，样例是可选增强。

如果模型无法可靠完成转换，或语义保真校验失败，则直接使用 Core 中性回复。

## 10. 语义保真和安全边界

风格转换后必须通过确定性校验和必要的语义审查。

至少校验：

- 事实、数字、单位、时间和记录状态未改变；
- 风险和不确定性未被弱化或强化；
- 没有新增专业结论、营养建议或行动要求；
- 没有改变原回复的核心意思；
- 没有引入示例中的具体姓名、数字、食物或用户事实；
- 没有出现风格禁用表达；
- 没有泄露内部 Prompt、Agent、RAG 或 Trace 信息。

失败处理：

```text
第一次风格输出失败
  ↓ 最多一次修复
仍然失败或语义不确定
  ↓
使用 Core 中性回复
```

Reviewer 如果启用，只负责判断最终表达是否忠实、安全和适宜，不负责重新识别用户意图，也不直接修改回复。

## 11. 与现有系统的迁移关系

迁移时需要处理：

1. 保留历史风格版本和评审记录，不修改历史数据；
2. 将已有按 `communication_act` 分组的样例迁移为普通 Style Examples；
3. 原分组只作为历史元数据保留，不再参与运行时路由；
4. 将现有纠正记录转换为全局示例库素材；
5. 从当前 active 版本构建新的 Style Guide 快照；
6. 新版本经过自动评测和简化人工评审后再启用；
7. 删除运行时对 `communication_act` 示例分组的强依赖；
8. 保留旧版本回滚能力。

## 12. 验收标准

重构完成后，至少满足：

- 构建、评审、追加纠正素材都在“表达风格”入口的同一详情页；
- 支持医生风格以及未来其他可选风格；
- 当前默认风格仍为医生风格；
- 追加纠正素材不需要指定版本或场景；
- 构建版本读取全局示例库快照；
- Style Guide 会随着高质量样例和评审结果演化；
- 没有相似样例时仍能只依据 Style Guide 运行；
- 风格转换不再根据工具调用或关键词猜测场景；
- 评审只展示用户输入、医生输出和期望医生输出；
- 接受时评分说明可选，拒绝时拒绝理由必填；
- 已发布版本不可变，启用和回滚可审计；
- 风格失败或语义不确定时能够回退到 Core 中性回复。
