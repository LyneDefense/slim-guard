# 表达风格：实现与操作手册

本文描述 2026-09-18 的本地代码实现，不代表服务器已部署。主设计为 [风格训练器与共享审查](STYLE_TRAINER_SHARED_REVIEW_DESIGN.md)；[旧评测草案](STYLE_EVALUATION_ITERATION_DESIGN.md) 仅供历史参考。

## 1. 日常操作

入口是 [表达风格](https://enceladus.online/admin/styles)。选择医生风格后只有三个子 Tab：

1. **追加纠正素材**：手工填写用户输入、已经发生的医生回答、期望回答、纠正意见。前两项必填，后两项至少填一项。只有负面意见也能提交，不自动伪造期望回答。
2. **构建版本**：点击构建，冻结“未参与构建”和“已参与构建”两个分组的全部现存素材，以及既有人工反馈。素材不是训练命令；模型不会执行素材中的指令。
3. **评审版本**：评审约 12 条独立测试的实际输出，而非把素材逐条重评。查看用户输入、必要语境、医生回答；原稿、对照回答、内部审查与修复可展开。仍是风格匹配、语义忠实、表达适宜三项评分。
4. 接受时说明可空；拒绝必须填理由，期望回答可选。可指出是回答、测试题还是自动审查的问题。只有回答反馈用于下一次风格构建；有争议的题目／判决不会被当作新风格素材或旧故障回归。它们仍阻止当前发布，不直接覆盖自动结果。接受不把模型回答自动加入正面素材。
5. 自动检查及回归通过、全部人评接受后，点击**采纳并发布**，然后单独**启用此版本**。构建完成或“建议采用”不自动启用。

新增或修改素材不会马上影响线上。修改生成新修订，重新进入“未参与构建”；已有风格包使用冻结副本，不会跟着素材变化。排除、冲突等质量结果也不会从全库消失，下次仍会重新分析。

本阶段训练是优化表达指引和固定示例，不更新基础模型权重，不是声音克隆式训练。

## 2. 后台能看到什么

“构建版本”内显示任务总览、阶段记录、可展开的阶段产物及最终结论：

| 阶段 | 可核对内容 |
| --- | --- |
| 冻结 | 全库和人评反馈数量、新增／修订数、素材快照、对照版本 |
| 素材审查 | 已分析／总数、逐条质量角色、处理原因、原始四字段 |
| 造题 | 独立开发题及最终验收题、语境和中性原稿、质量检查与修复 |
| 联合构建 | Guide、规则边界、证据片段和修订、反例、固定示例 |
| 一致性检查 | 对全部素材分批核对的结论；失败原因送回候选修复 |
| 自动优化 | 每轮候选、真实规则／示例差异、同题比较、保留或淘汰结果 |
| 最终验收 | 中性原稿、对照与候选回答、内部审查、每次修复输出及兜底 |
| 结论 | 建议采用／无明确提升／不建议采用／需要人工确认、退步与限制 |

四维自动评分是语义忠实、风格匹配、自然程度、表达适宜。报告显示 1～5 分分布，以及更好／更差／持平／不确定、失败、修复和兜底计数。历史拒绝用例的回归单独列出，不冒充独立验收题。低分或自动失败不会被过滤成人评只看好回答。

页面约每 2.5 秒刷新任务。等待模型时显示用途、开始时间、等待秒数和超时上限；worker 心跳与产物进展分开。关闭或刷新网页不停止任务。网络读取失败或心跳过期会显示“状态待确认”，不是直接宣告后台失败。

失败和取消保留已完成产物。恢复沿用同一快照和剩余预算；修改素材／模型后应新建任务，恢复不会偷偷使用新配置。预算耗尽或两次产物修复仍失败时，新建任务前先处理明确原因，不反复点恢复。

## 3. 训练与测试隔离

执行顺序为：

```text
冻结全库／人工反馈／基线／预算／模型与审查签名
  → 分批分析全部素材
  → 冻结人工表达参照
  → 独立生成并核对开发集和最终验收集
  → 联合构建 Guide＋固定示例
  → 全库一致性审查（最多一次带原因修复）
  → 开发题＋历史拒绝题比较（预算内最多 N 轮）
  → 选定一个最终候选
  → 最终验收只评这个候选
  → 报告＋约 12 条最终人评
```

素材质量包括正面表达、负面意见、内容纠正、语义漂移、重复、冲突、缺少上下文、不可用。这些不是“询问／鼓励”等用户意图标签。

只有合格正面表达对能进入固定示例。负面意见可以支持有边界的规则，不自动制造正面回答。每条规则必须引用真实素材 ID、修订及逐字证据；固定示例必须与规则证据相连。最终产物还会逐批对全库核对，包含没有入选或被隔离的素材。

Guide 与示例联合生成、独立一致性审查，不是“所有素材随便生成 Guide，再随机挑 8 条”。没有固定示例数量；使用固定 Prompt 字节预算，超限必须收敛候选，不能线上静默截断。

开发集默认 6～10 题，最终验收默认 12 题（契约允许 10～15）。数字是工程与人评负担设置，不是统计可靠性的保证。禁止原素材复述、开发／验收题族重叠、仅替换数字的重复题；另有模型审查语义近重复、语境与中性原稿一致性。已被消费的旧测试题族不再算新验收。

生成题目不接收候选 Guide、人工期望答案或当前候选得分。优化器只接收开发结果／历史反馈，不能根据本轮最终验收结果再选候选。合成照片描述不是调用过真实视觉模型；训练不写健康记录、记忆、RAG 或用户消息。

两版用相同模型配置、审查策略、输入及原稿重跑。比较裁判不见版本标签，以固定人工表达参照判定，不拿候选自己的 Guide 当“更像医生”的唯一标准。无正面参照或结论不确定时，不能自动宣称提升。同模型独立调用仍可能有相关偏差，最终人评保留。

## 4. 线上执行

版本是不可变的 `StylePackage`：Guide、固定示例及顺序、确定性编译 Prompt、版本与归属、Hash。证据和原始素材不会整库复制到线上 Prompt。

每轮锁定已启用的发布包，组装当前用户输入、必要短语境、已确定的中性原稿／内容契约：

```text
Style Agent 改写
  → 共享完整性／语义／表达审查
  → 通过：采用
  → 需修复：原稿＋失败输出＋具体原因，再改写一次
  → 仍未通过／不可可靠检查：中性原稿兜底并记录原因
```

最多两次生成，最多四次模型调用，审查与修复共用节点／整轮预算。未经审查的回答不能当成功。确定性检查保护引用、数值、版本及上游明确保护的内容块；一般语义由独立模型审查，不再用“建议／需要”等词表替代含义判断。

共享审查不改写回答、不更改 Guide、不调用业务工具；其策略独立版本化。训练通过同一 Style Agent 调用同一审查组件，线上不调用训练器。运行时不查素材表、不进行风格向量或词法检索。营养 RAG 的向量索引、COS 和检索不受影响。

发布不等于启用；启用指针变化只影响新调用，已锁定上下文的重试不切换版本。当前默认医生风格，移动端个人选风格不在本次范围内。

## 5. 模块与数据

| 目录／模块 | 唯一职责 |
| --- | --- |
| `expression_style/package.py` | Guide、证据、固定风格包、确定性编译与 Hash |
| `expression_style/contracts.py` | 改写上下文和只读版本端口 |
| `expression_style/trainer/` | 素材、联合构建、数据集、评测、优化、预算与持久化端口 |
| `expression_style/review/` | 共享判决、完整性检查、独立审查策略 |
| `agents/style/` | 同一在线／离线改写闭环 |
| `style_management/corpus.py` | 人工素材及修订 |
| `style_management/builds.py` | 冻结构建、分页过程和产物、取消与恢复 |
| `style_management/versions.py` | 最终人评、发布与启用 |
| `style_management/job_store.py` | 带所有权校验的任务存储与事件 |
| `style_management/worker.py` | 薄任务调度、租约与独立心跳 |
| `style_management/runtime.py` | 只读已启用发布包 |
| `frontend/src/components/style/` | 风格列表、三个子页及过程／报告组件 |

五张业务表：

- `expression_styles`：风格及启用指针。
- `expression_examples`：四字段人工素材、当前修订及参与结果。
- `expression_build_runs`：输入快照、检查点、事件、预算、租约和报告。
- `expression_versions`：最终不可变风格包，不存中间任务状态。
- `expression_review_cases`：独立测试、两版真实输出、自动检查和人评。

同风格只允许一个排队／运行任务；幂等键防重复提交。worker 每 15 秒独立心跳，租约 90 秒，过期可以被新 worker 接管；旧所有权不能覆盖新结果。成功检查点恢复复用，不重复追加版本或素材。

API 统一为 `/api/admin/styles`，保留管理员认证和写请求 CSRF。主要资源为 `examples`、`builds`、`versions`、`reviews`；任务的 `events` 支持游标、`artifacts` 支持分页。摘要不返回完整快照、内部模型缓存或 worker 令牌。

## 6. 配置和成本

继续使用 `ZHIPU_API_KEY`、`ZHIPU_BASE_URL`、`ZHIPU_TEXT_MODEL`。无需新的服务或密钥。

```dotenv
STYLE_ITERATION_WORKER_ENABLED=true
STYLE_ITERATION_POLL_SECONDS=2
STYLE_TRAINING_MAX_ROUNDS=2
STYLE_TRAINING_MAX_CALLS=350
STYLE_TRAINING_MAX_TOKENS=1000000
STYLE_TRAINING_MAX_SECONDS=3600
```

四个 `STYLE_TRAINING_*` 配置有上述默认值；无需为了启动补填。单请求默认 120 秒、100,000 UTF-8 字节；固定风格 Prompt 上限 24,000 字节。它们是工程上限，不代表每次都会耗满。总 Token 成本包含素材分析、造题、构建、两版改写、共享审查和比较裁判，不只是最终人评的 12 条。

预算在创建任务时冻结。恢复不会清空调用／Token／已记录运行时间；预算耗尽不等于完成，不会产生可发布的假报告。

正常对话仍要求原有运行开关：

```dotenv
AGENT_RUNTIME_MODE=harness
MULTI_AGENT_MODE=on
STYLE_RENDER_ALL_NORMAL_REPLIES=true
RESPONSE_REVIEWER_ENABLED=true
```

风格启用以数据库为准，没有第二个环境变量版本选择器。不要删除营养 RAG 的 embedding 配置；仅风格专用检索调用被删除。

## 7. 开发期迁移与部署注意

新迁移 `20260918_03_style_trainer` 会进行用户授权的风格子系统清理：

- 保留风格条目和人工素材的输入、回答、期望、意见及来源；清除旧参与结果，重新进入未参与分组。
- 删除旧构建产物、版本、关联评审及旧版本表中的向量／快照字段。
- 不把旧机制自动生成的 `accepted_review` 回答当真人素材保留。
- 清空原启用指针，为医生风格启用 `doctor_builtin_v1` 基础包；**这不是此前训练好的专属医生语气**。需要新构建、人评、发布和启用。
- 不清理账号、聊天、健康数据、个人记忆、营养 RAG 或 COS 对象。

本轮没有对真实本地数据库或服务器执行迁移，也没有自动部署。已发布数据库升级时必须先备份、使用维护模式，避免旧进程访问已替换表：

```bash
ssh me
cd /home/ubuntu/slim-guard
git pull --ff-only
./deploy.sh --maintenance
./deploy.sh status
```

上述步骤仅在代码已推送且确定要部署后执行。首次结构切换不能只靠旧镜像回滚；恢复旧版本需匹配部署前数据库备份。被删除的旧派生数据只能从备份找回。

## 8. 验证边界

自动测试使用临时数据库和受控假模型，验证真实 worker／API／Agent 调用链、预算、隔离、恢复、发布及迁移行为。它们不能证明真实模型已经准确复刻医生语气。

本轮实际校验记录（非全量测试）：核心风格、编排、权限、迁移与配置定向集合 134 项通过；随后保护契约补强的 35 项、评审争议隔离的 9 项、迁移与版本切换的 4 项专项通过，专项与前面的覆盖有重叠。前端风格回归 8 项通过；Ruff、Mypy、TypeScript 检查与生产构建通过。未执行云端验收、真实模型质量测量或全量业务测试。

首次真实构建仍需检查素材处理、测试题质量及最终人评；共享审查的模型误报／漏报率需要真实标注用例校准，不能把契约测试通过当作模型质量分数。当前后台通过拒绝框的分类记录坏题／自动判决问题并阻止发布；尚无独立的“修订单道验收题后局部重跑”入口，需重新构建。后者是设计中的进一步细化能力，不假称已经实现。
