# 表达风格：实现与操作手册

## 入口和日常操作

打开 [表达风格](https://enceladus.online/admin/styles)，选择“医生风格”。左侧不再有 A/B 人评、风格版本、风格纠正三个旧入口。

1. **追加纠正素材**：只填用户输入、已经发生的医生回答、期望医生回答。提交到该风格的完整素材库，不选择版本或场景。下半页可搜索、分页、按来源/状态/质量分类筛选，查看纳入过的版本，排除或重新纳入素材。
2. **构建版本**：点击构建，冻结所有 pending/approved 素材和既有评分反馈。后台分批处理全部素材，不截断为固定 12 条。可展开构建过程，查看失败原因并重试同一快照，或补充素材后另建版本。
3. **评审版本**：选择版本，比较用户输入、实际生成的医生回答、期望回答。按风格匹配、语义忠实、表达适宜打 1～5 分。接受不要求说明；拒绝会弹出必填理由框。原文及自动语义检查在折叠区域。补充期望回答会加入下一次构建素材。
4. 所有样例自动检查通过、人工全部接受后，**采纳并发布**。再单独点击**启用此版本**。页面启用立即作用于新回复，不需要修改环境变量或重新部署。旧的已发布新制版本可以重新启用，以回退表达风格。

构建不是微调基础模型。产物是 Style Guide、样例/向量和评审集组成的固定版本。追加素材、修改评分、排除示例都不会直接修改已发布版本。

## 数据和模块

| 模块 | 职责 |
| --- | --- |
| `style_management/contracts.py` | 三字段素材、三个评分、Style Guide 和质量分析契约 |
| `style_management/models.py` | 新风格、跨版本示例库、版本快照、版本评审四张表 |
| `style_management/repository.py` | 风格隔离、冻结素材、评分、发布门槛和启用指针 |
| `style_management/worker.py` | 可恢复的后台构建，租约、阶段事件、分批分析和评测 |
| `style_management/runtime.py` | 只读取已发布版本；构建评测使用同一 Style Agent |
| `agents/style/` | 纯表达改写、相似样例检索、独立语义判断、一次重试和中性稿兜底 |
| `agent_models/embeddings.py` | 共享向量接口/智谱实现；风格和营养 RAG 的索引与数据仍相互独立 |
| `frontend/src/components/style/` | 风格列表、三个子页、专属 API、类型和样式 |

新表只有 `expression_styles`、`expression_examples`、`expression_versions`、`expression_review_cases`。身份、健康记录、个人记忆、营养 RAG 不写入这些表。

版本状态：`queued → building → ready_for_review → published`；失败进入 `failed`，可重试。是否启用由风格的 `active_version_id` 表示，不把“已发布”和“当前启用”混为一谈。

构建时用行锁和唯一索引限制同一风格只有一个未完成任务。worker 用带所有权令牌的 5 分钟租约；每批/条更新租约，进程退出后可接管过期任务。发布和评分持有版本锁，已发布版本不能继续评分或覆盖产物。

## 构建与运行时

- 模型逐条提取表达差异，标记 `expression / content_change / conflict / unusable`。这些是**素材质量标签**，不是用户意图或话术分类。
- 有具体事实变化的素材不能作为纯表达样例。每条规则记录证据 ID；不足两个不同文本样例的规则降为候选，冲突规则显式显示。运行时只采用稳定规则，不把单例偏好当成普遍要求。
- 候选版本逐条实际生成回复，再运行自动校验。本条期望回答和同原文重复样例不会作为该条评测的提示答案。人工依然需要判断风格是否合适，自动通过不是发布批准。
- 人工接受且自动通过的结果成为正向素材；拒绝结果不进入正向素材。改变评审结论会同步排除先前的正向素材；评分和拒绝原因保留在后续构建快照。
- 线上先锁定默认风格的已启用版本，再按中性原文检索 0～3 条表达样例。当前支持多风格管理，移动端个人选风格仍是后续能力；默认仍为医生风格。
- 检索使用中文相邻字对匹配与向量余弦相似度，合并排序；词法相似度至少 0.2 或向量相似度至少 0.82 才入选。向量模型使用智谱 `embedding-3`、1024 维，构建时保存向量，在线只嵌入查询。模型签名或维度不符不比较向量。查询向量最多等待 4 秒，失败后仅走词法；没有可靠匹配时只用 Guide。
- Style Agent 最多两次改写，每次在内部运行确定性保护和独立的语义审查模型调用，最多 4 次模型请求。重试包含失败原因；仍不通过则原样返回 Core 中性稿，不再次执行业务工具。
- 语义审查检查关键对象、事实、数量、状态、风险、限定条件、不确定性与新增建议，不猜用户意图。调用受整轮和节点预算约束，不能绕过权限或无限重试。检查和返工信息保存在对应回复 Artifact 的 `internal_checks`。
- `slimguard_default_v1` 仅作为代码内的安全表达兜底，不是旧数据库风格版本，也不读取旧样例。正常默认版本来自新表的启用指针。

## 配置

沿用正常对话的 `ZHIPU_API_KEY`、`ZHIPU_BASE_URL`、`ZHIPU_TEXT_MODEL`，不再需要离线 `STYLE_CORPUS_*` 密钥。

```dotenv
STYLE_ITERATION_WORKER_ENABLED=true
STYLE_ITERATION_POLL_SECONDS=2
AGENT_RUNTIME_MODE=harness
MULTI_AGENT_MODE=on
STYLE_RENDER_ALL_NORMAL_REPLIES=true
RESPONSE_REVIEWER_ENABLED=true
```

`DEFAULT_STYLE_PROFILE` 已删除：启用版本以网页和数据库为准，环境变量不再是第二个控制入口。`MULTI_AGENT_MODE=off` 仍会关闭整条最终风格管线。

## 本次开发期重置与部署

按用户最新明确要求，不保留旧风格系统或历史审计。迁移 `20260918_02_reset_expression_styles` 删除旧的 12 张风格表及专属触发器函数，旧 CLI、A/B 接口、场景分类契约、兼容页面均已删除。旧迁移编号仅在迁移账本中保留，避免已部署数据库重复执行其他模块迁移。

旧风格版本、评分、纠正素材和迭代历史会清空，不把场景目标伪造成用户输入，也不自动宣称旧 v3 在新系统中通过评审。新库初始化 `doctor_builtin_v1`：仅基础医生式表达指引，**没有训练样例，不等于此前人工调校的 v3**。要获得专属风格，按上方流程追加素材、构建、评审、发布、启用。

账号、消息、健康记录、个人记忆、RAG 资料/语料版本不在清理范围。部署脚本照常备份数据库；旧风格记录如需找回只能从部署前备份中恢复，不是切回旧镜像就能恢复。

首次切换必须先构建镜像，再停止旧 app，最后执行迁移和启动新服务，避免旧进程在删表后继续访问旧表。`./deploy.sh` 支持 `--maintenance` 完成这一切换（见部署脚本）。日常后续部署仍直接运行 `./deploy.sh`。

```bash
ssh me
cd /home/ubuntu/slim-guard
git pull --ff-only
./deploy.sh --maintenance
./deploy.sh status
```

该重置不能通过旧镜像自动回滚。发生故障时优先修复前进；确需旧系统，必须在停服后同时恢复部署前数据库备份和对应旧镜像。

接口统一前缀 `/api/admin/styles`，沿用管理员会话认证和写请求 CSRF 保护。构建、评分、发布、启用只对管理员开放。表单中请提供必要且脱敏的测试数据，不上传整段私聊或密钥。
